# TEMPLATE — do not apply as-is and do not commit real values.
#
# Create the real Secret out-of-band, e.g.:
#
#   kubectl create secret generic claude-shipyard-secrets \
#     --from-literal=CLAUDE_CODE_OAUTH_TOKEN="$(claude setup-token)" \
#     --from-literal=GITHUB_TOKEN="$GITHUB_TOKEN" \
#     --from-literal=GH_TOKEN="$GITHUB_TOKEN"
#
# `make deploy-k8s` deliberately does NOT apply this file.
apiVersion: v1
kind: Secret
metadata:
  name: claude-shipyard-secrets
  labels:
    app.kubernetes.io/name: claude-shipyard
type: Opaque
stringData:
  # Claude Code subscription OAuth token — generate on a host with `claude setup-token`.
  CLAUDE_CODE_OAUTH_TOKEN: "replace-me"
  # Token for git/gh access from inside the container (optional).
  GITHUB_TOKEN: "replace-me"
  GH_TOKEN: "replace-me"
