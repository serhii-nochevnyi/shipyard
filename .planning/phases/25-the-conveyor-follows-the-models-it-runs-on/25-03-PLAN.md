---
phase: 25
plan: 03
title: "The prose names the models the runtimes actually run"
type: implementation
wave: 9
depends_on: [T-25-02, T-24-07]
files_modified:
  - CLAUDE.md
  - README.md
  - plugins/delivery-pipeline/commands/deliver.md
  - plugins/delivery-pipeline/commands/investigate.md
  - docs/gsd_multilevel_delivery_pipeline.md
  - plugins/delivery-pipeline/scripts/pipeline-config.cjs
  - tests/unit/pipeline-config.test.cjs
requirements: [REQ-28]
delivery:
  ticket: T-25-03
  risk: low
  human_checkpoint: false
---

## Goal

Every sentence about models in the repository is true on 2026-09-07: the Agent
tool's `model` parameter takes ONLY the four aliases (enum-validated — the
existing sentences are right), while subagent FRONTMATTER also takes full ids
and `inherit`; `opus` is Opus 5 from Claude Code 2.1.219; `fable` is
Fable 5.1 from 2.1.255 and stays the default for the two judgment roles, with
the consent hazard named; GSD 1.13's `gate-status:` commit trailer is not the
conveyor's `gate_status:` PR trailer.

## Context (Reads)

- ADR-003 D3/D4. The user's decision (2026-09-07): fable stays for
  `arch-review`/`integrator`; the DOCS change, not `ladderTier`.
- **Do NOT "fix" the alias claims — they are TRUE.** `CLAUDE.md` ~66
  ("validates `model` against exactly that set"), `deliver.md` ~241 ("rejected
  on input validation"), `investigate.md` ~61,
  `docs/gsd_multilevel_delivery_pipeline.md` ~664 and `README.md` ~199 all say
  the Agent tool rejects a full model id. Verified against the LIVE Agent tool
  schema on CLI 2.1.263 (2026-09-07): `model` is
  `enum ["sonnet","opus","haiku","fable"]`. The model-config docs list full ids
  and `inherit` for subagent FRONTMATTER (`.claude/agents/*.md`) and other
  surfaces — a different surface from the tool parameter shipyard dispatches
  through. An earlier draft of this plan and of ADR-003 conflated the two and
  told an executor to delete a true sentence; both were corrected on
  2026-09-07 before this ticket ran. What those five sentences actually LACK is
  the distinction: add one clause naming frontmatter as the surface that does
  accept full ids, so the next reader cannot repeat the conflation.
- Contradictions to resolve toward the code: `CLAUDE.md` ~66 "it is paid, so
  it is opt-in only", `deliver.md` ~247 "never a default", `README.md` ~199 —
  versus `pipeline-config.cjs ladderTier` (~409) `if (JUDGMENT_ROLES.has(role))
  return topTier(cfg)` → `fable` on Claude. Write what the code does and WHY
  (1M window for whole-diff review), and add the hazard: Fable may bill usage
  credits and asks consent once; in a background/Remote Control session the
  prompt waits `dialogExpiry` (5 min) and then ends the turn without sending —
  so an org that has never consented should set `pipeline.models.arch-review`
  and `.integrator` to `opus` until someone has answered the prompt once
  interactively.
- "Claude Fable 5" → "Claude Fable 5.1" in `pipeline-config.cjs` ~52,
  `deliver.md` ~245, `README.md` ~199 (comments/prose only — no behaviour).
- `README.md` ~237 and ~368 still tell the reader to install
  `@opengsd/gsd-core@1.7.0`; T-25-01 moved the pins to 1.13.0 without touching
  the README (T-24-07 owned it) — update both mentions here, and add one
  sentence that `opus`/`fable` inside the image mean Opus 5 / Fable 5.1 from
  Claude Code 2.1.263 on.
- GSD 1.13 `ship.md` reads a `gate-status:` COMMIT trailer for its TDD audit.
  Add one line where the conveyor's `gate_status:` PR-body trailer is first
  introduced (`deliver.md` Step 4c, `README.md`): different mechanism,
  different spelling, do not confuse the two.
- Cross-phase parent T-24-07 owns `deliver.md` until phase 24 lands; T-24-05
  owns `pipeline-config.cjs`/its test. This ticket waits for that epic by
  design.

## Scope

- The rewrites above. A pinned test in `pipeline-config.test.cjs` that
  `arch-review`/`integrator` resolve to `fable` on `runtime: claude` and to the
  capped tier on `codex` — the decision as a test, so the next prose drift
  is caught by `grep` of a fact the test guarantees.
- No behaviour change to `pipeline-config.cjs` beyond comments.

## Out of scope

`best` as an Agent alias (not accepted by the Agent tool's `model` field nor
by GSD's alias set — record, do not adopt). Emitting full ids (policy stays
aliases).

## Acceptance criteria

- The alias sentences SURVIVE: `grep -n "rejected on input validation" plugins/delivery-pipeline/commands/deliver.md` still matches, and each of the five sites now also names subagent frontmatter as the surface that accepts full ids.
- `grep -rn "Fable 5\b" CLAUDE.md README.md plugins/delivery-pipeline` finds only "Fable 5.1".
- `grep -n "opt-in only\|never a default" CLAUDE.md README.md plugins/delivery-pipeline/commands/deliver.md` is empty (the sentences now describe the default and the opt-OUT).
- `deliver.md` and `README.md` each contain one sentence contrasting
  `gate_status:` (PR body, conveyor) with `gate-status:` (commit trailer, GSD).
- `grep -n "gsd-core@1.7.0" README.md` is empty.
- `make test-docs` passes; `node tests/unit/pipeline-config.test.cjs` passes
  with the new pinned case.

## Test strategy

Contract greps plus one unit case. No behaviour.

## Verification commands

- `node tests/unit/pipeline-config.test.cjs`
- `node --check plugins/delivery-pipeline/scripts/pipeline-config.cjs`
- `grep -n "rejected on input validation" plugins/delivery-pipeline/commands/deliver.md`
- `grep -c "frontmatter" CLAUDE.md plugins/delivery-pipeline/commands/deliver.md`
- `make test-docs`
