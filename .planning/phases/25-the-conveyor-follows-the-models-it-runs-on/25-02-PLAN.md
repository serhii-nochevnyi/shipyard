---
phase: 25
plan: 02
title: "A Codex agent carries the model its effort needs"
type: implementation
wave: 8
depends_on: [T-25-01, T-24-07]
files_modified:
  - scripts/gen-codex-shipyard.cjs
  - plugins/delivery-pipeline/scripts/pipeline-config.cjs
  - capabilities/delivery-pipeline/capability.json
  - plugins/delivery-pipeline/commands/deliver.md
  - plugins/delivery-pipeline/references/pr-sentinel.md
  - tests/smoke/codex-shipyard-smoke.sh
  - tests/unit/gen-codex-shipyard.test.cjs
  - tests/unit/pipeline-config.test.cjs
  - CLAUDE.md
  - README.md
requirements: [REQ-27]
delivery:
  ticket: T-25-02
  risk: medium
  human_checkpoint: false
---

## Goal

Every Codex agent carries the model the depth of its role needs, chosen from a
palette the operator declares rather than from GSD's default tier map, plus the
effort that depth is expressed at. The four roles that escalate get a second
agent file at the palette's deepest entry, because on Codex an agent is a FILE
and a signal cannot reach one that does not exist.

*(Re-planned 2026-09-07 from ADR-005 D6–D9, before any code was built. The
first version of this ticket said to write NO model at all and let the
operator's `config.toml` default apply. That was correct only while effort
could carry the whole depth range; the operator's palette caps `gpt-6-astra` at
`high` effort, so depth above that has to come from the model again.)*

## Context (Reads)

- ADR-003 D2; `CLAUDE.md` "Models are TWO layers" and "`fable` is
  Claude-only" — the paragraph that argues FOR baking ("Without that, all
  seven Codex agents ran at the CLI default and arch-review was the same model
  as drift-check") must be rewritten by this ticket, because its premise is
  gone: `pipeline-config.cjs capForRuntime` (~line 386) caps every role to the
  workhorse tier on Codex, so all seven agents already get the SAME model and
  differ only by effort. Measured 2026-09-07 in `~/.codex/agents/`: seven
  files, seven `model = "gpt-5.6-terra"`, efforts low/high/xhigh.
- `scripts/gen-codex-shipyard.cjs codexModelFor` (~95–125): resolves the tier
  through `pipeline-config.cjs` with `runtime: codex` forced, then reads
  `catalog.runtimeTierDefaults.codex[tier].model` DIRECTLY from
  `~/.codex/gsd-core/bin/shared/model-catalog.json`. It never consults GSD's
  remap keys (`model_policy.runtime_tiers.codex.<tier>`,
  `model_profile_overrides.codex.<tier>`) — the audit (F23) confirmed a custom
  sonnet remap did not change the generated files, though `CLAUDE.md` says a
  remap "takes effect".
- GSD 1.13 `model-resolver.cjs` (in `~/.codex/gsd-core/bin/lib/` after the
  installer runs) is the resolver to call; its `resolve_model_ids: "omit"`
  posture is why GSD's own `gsd-*.toml` carry no `model` key. `deliver.md`
  (~line 344) already states ours carry none — code and prose disagree; make
  the code match the prose (deliver.md itself is T-24-07's / T-25-03's file and
  is NOT edited here).
- The user's Codex default lives in `~/.codex/config.toml` (`model =
  "gpt-6-astra"` here). Never read it into a generated file — a baked copy is
  the defect in a new coat. Do not hardcode any model id in shipyard.
- Effort: Astra supports low…max (no `ultra`); `resolveEffort` clamps `max` →
  `xhigh` on Codex — keep the clamp.
- `tests/smoke/codex-shipyard-smoke.sh` (~91–99) asserts the resolver emits
  tier aliases; grep it for `model =` assertions before editing — the T-24-02
  trap. A NEW unit test `tests/unit/gen-codex-shipyard.test.cjs` drives
  `codexModelFor` with a fixture HOME (a fake `gsd-core` with a resolver stub)
  so the rule is tested without network.

## Scope

- **The palette, in `pipeline-config.cjs` and declared in `capability.json`.**
  `codex_models` is an ORDERED list of `{ model, max_effort }`, shipped with the
  operator's palette as the default:
  `[{gpt-6-astra, high}, {gpt-5.6-terra, max}, {gpt-5.6-sol, ultra}]`. A
  reader that finds the key absent falls back to that default; a malformed entry
  warns and is skipped, and an empty palette means "write no model", which is
  the previous behaviour and a safe floor. `ultra` joins `EFFORTS` for Codex
  only, and the `max` → `xhigh` clamp for `runtime: codex` is deleted (ADR-005
  D7: both halves of its comment are false — GSD's
  `codexModelEffort._baseline` advertises `max` for every model, and
  `advertisedCodexEffort` returns that baseline for a model it does not name,
  which is what `gpt-6-astra` is). `minimal` → `low` stays.
- **`codexModelFor(role)` → `{ model, effort }`, by the selection rule.** The
  effort comes from `resolveEffort` as it does today. The model is then: for a
  JUDGMENT role, the palette's DEEPEST entry; for every other role, the
  SHALLOWEST entry whose `max_effort` covers that effort. A role whose effort no
  entry can carry takes the deepest entry and says so on stderr — never a
  reduced effort, because losing depth silently is the defect this ticket
  exists to remove. A GSD remap key (`model_policy.runtime_tiers.codex.<tier>`,
  `model_profile_overrides.codex.<tier>`) still WINS over the palette, resolved
  through GSD's own resolver rather than by reading `runtimeTierDefaults`
  directly — the claim `CLAUDE.md` already makes and the generator never
  honoured (audit F23).
- **Eleven agents, not seven (ADR-005 D8).** The generator writes a second file
  for the four roles that escalate — `shipyard-ci-fix-deep`,
  `shipyard-review-fix-deep`, `shipyard-pr-sentinel-deep`,
  `shipyard-arch-review-deep` — each at the palette's deepest entry and its top
  effort. The integrator gets no variant: one call per phase, already deepest.
  A `-deep` file is written only when the palette's deepest entry is deeper
  than the role's own, so a single-entry palette produces seven agents and no
  dead files.
- **The prose that makes them reachable.** `deliver.md` and `pr-sentinel.md`
  name the `-deep` agents and when to invoke them: `repeat_exhausted` for a
  repair role, a journalled prior `arch_review … verdict=violation` for the
  judge. Both files are the Codex skill's SOURCE, so the generated
  `~/.agents/skills/shipyard-*/SKILL.md` carries it; without this the variants
  exist and nothing calls them. Keep the sentences free of the word "Claude" in
  any runtime-contrasting sense — the generator substitutes it (`CLAUDE.md`,
  "The Codex generator rewrites the word Claude in prose").
- `CLAUDE.md` and `README.md`: replace the baking paragraph with the palette,
  the selection rule, the eleven agents, and the fact that a remap now wins.

## Out of scope

`pipeline-config.cjs` (the cap policy stands); `deliver.md` (T-25-03);
whether Astra should be a GSD tier (GSD's catalog).

## Acceptance criteria

- With the default palette and no remap, the generated files carry exactly:
  drift-check astra/low, research astra/high, ci-fix astra/high, review-fix
  astra/high, pr-sentinel astra/high, executor terra/xhigh (the generator has
  no risk signal, so it bakes the medium baseline), arch-review sol/max,
  integrator sol/max. Today all seven carry `gpt-5.6-terra`, so the test fails
  on current code.
- The four `-deep` files exist and carry sol/ultra; `shipyard-integrator-deep`
  does NOT exist.
- A palette of one entry (`[{gpt-6-astra, high}]`) produces seven agents, no
  `-deep` file, and every role at `high` or below with a stderr line naming each
  role whose effort the palette could not carry.
- `model_policy.runtime_tiers.codex.sonnet = "x-model"` in the fixture defaults
  wins over the palette for the roles that resolve to that tier.
- `resolveEffort` with `runtime: codex` returns `max` for an effort of `max` and
  `ultra` for `ultra`; today `max` returns `xhigh`, so this fails on current
  code. `minimal` still returns `low`.
- No model id appears in `scripts/`, `plugins/` or the docs outside the palette
  default and the comments that quote a measurement.
- `deliver.md` and `pr-sentinel.md` each name all four `-deep` agents and the
  verdict that selects them; the generated Codex skill contains them too
  (assert against the throwaway HOME the smoke builds).
- `make test-codex-shipyard` passes.

## Test strategy

Unit-test `codexModelFor` against a fixture HOME; keep the smoke as the
end-to-end check. TDD: assert "no model line" first and watch it fail against
the current generator.

## Verification commands

- `node tests/unit/gen-codex-shipyard.test.cjs`
- `node tests/unit/pipeline-config.test.cjs`
- `node --check scripts/gen-codex-shipyard.cjs`
- `node --check plugins/delivery-pipeline/scripts/pipeline-config.cjs`
- `! grep -rn "gpt-5\.6\|gpt-6" plugins/delivery-pipeline/scripts plugins/delivery-pipeline/commands plugins/delivery-pipeline/references`
- `make test-docs`
- `make test-codex-shipyard`
