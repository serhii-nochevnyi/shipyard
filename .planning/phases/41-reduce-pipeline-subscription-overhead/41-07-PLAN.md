---
phase: 41
plan: 07
title: "Integrate phase 41 with trusted outcome and efficiency evidence"
type: integration
wave: 3
depends_on: [T-41-01, T-41-02, T-41-03, T-41-04, T-41-05, T-41-06, T-41-08]
files_modified:
  - .planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md
requirements: [REQ-151, REQ-152, REQ-153, REQ-154, REQ-155, REQ-156, REQ-157]
autonomous: false
estimate: {tokens: 10000, raw_tokens: 10000, tasks: 3, confidence: low}
delivery:
  ticket: T-41-07
  risk: high
  human_checkpoint: true
must_haves:
  truths:
    - "REQ-151 through REQ-157 are evidenced against the integrated phase head, ticket set and signed/verified outcomes."
    - "Parent and child work, failed/parked/recovery work and quality signals remain in the cohort evidence."
    - "A verified functional phase may pass with an explicitly inconclusive savings verdict under existing readiness rules."
  artifacts:
    - .planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md
  key_links:
    - "all terminal ticket heads -> epic combined diff -> independent integrator -> sealed INTEGRATION.md -> human release gate"
---

## Goal

Close phase 41 only after the seven requirements work together on the phase epic and an independent integrator seals the outcome. Savings may remain inconclusive when the approved live sample is not ready; functional and safety gates still decide whether the phase can ship.

## Context (Reads)

- `.planning/architecture/ADR-019-pipeline-subscription-efficiency.md:29-55`; `CONTEXT.md` P41-A–F; `41-RESEARCH.md:326-339` validation matrix.
- `plugins/delivery-pipeline/commands/deliver.md:2735-2785` is the existing phase integrator and seal flow; `plugins/delivery-pipeline/scripts/claude-role-host.cjs:548,677-728` owns bounded integrator input and `role-artifact.cjs:1136,1564-1616` validates the result. Read `.planning/phases/39-remove-conveyor-session-friction/INTEGRATION.md` for the existing combined head/base, ticket-set digest, findings and verdict shape.
- Read all seven phase-41 SUMMARYs, trusted dispatch and finalization receipts, installed identity observations, GSD projection diff/check evidence, and usage/quality report. Phase 40 remains pending and supplies no implementation prerequisite.

## Scope

- **Initial blocking checkpoint:** current graph readiness is insufficient because `state-sync` can release a diamond when parents are merely branched. Before drafting or dispatching integration, the trusted orchestrator must observe all seven source PRs merged into the phase-41 epic, fetch and pin that epic SHA, and prove each observed merge SHA with `git merge-base --is-ancestor <merge-sha> <epic-sha>`. Resolve identities from graph-scoped PR observations; do not accept agent claims. Missing, open, changed or non-ancestor parents keep T-41-07 parked at its first checkpoint. Revalidate the pinned epic identity before sealing.
- **Actual integration target:** invoke the existing independent phase-integrator against the pinned complete epic snapshot, using its host-prepared isolated read-only input. The T-41-06-based T-41-07 branch only carries INTEGRATION.md and is never the combined source input. Do not merge other parents into that carrier worktree or assume the pending phase-40 diamond fix. Record both carrier and integrated epic identities; the integrator seal binds the epic SHA. If the existing host cannot bind/read that exact snapshot, stop explicitly rather than reviewing the incomplete carrier branch.

- On an integrated phase-41 epic head, verify each ticket artifact and focused fixture result; inspect combined source for overlapping contract changes, owner/wake flow, prompt measurement, candidate recovery, projection dependencies and outcome reporting. Record epic head/tree/base, ticket set and digest, installed host/hook/policy identities and independent integrator result in `INTEGRATION.md`.
- Reconcile parent/child usage and verified ticket outcomes, including failed, interrupted, parked, recovery, wait polling and retries. Compare matched runtime/model/effort/role/account cohorts, output/cache categories, total and median/p90 per verified completion, and quality signals (false greens, skipped gates, lost constraints, duplicate work, reopens, escaped defects). If 20 verified completions, 95% attribution, output coverage or seven-day defect evidence is absent, mark savings **inconclusive**. Do not infer quota savings from token totals.
- Separate four release states for each capability: implemented, installed, behaviorally verified and efficiency measured. Record changed-source installed checks for role host and stop hook, then controlled first-response/wake observations. Source hash equality alone is insufficient.
- Use the existing independent integrator/sealer path, retain findings and exact ticket fixes, and stop at the high-risk human checkpoint before final phase handoff or merge. A failing integration verdict cannot be converted into a pass by the usage report.

## Out of scope

Product code or test changes in the integration ticket, phase-40 implementation, model floor changes, automatic handoff thresholds and financial savings promises.

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Require all seven merged parents in the actual integrator input</name>
  <what-built>Seven source PRs are merged into the epic and their merge SHAs are ancestors of its pinned SHA.</what-built>
  <how-to-verify>Read graph-scoped PR observations, fetch the epic and run git merge-base --is-ancestor for all seven observed merge SHAs. Confirm the integrator reads this snapshot, not the T-41-06-based carrier branch.</how-to-verify>
  <resume-signal>Resume only with seven merge/ancestry proofs and the exact bound integrator snapshot; otherwise remain parked.</resume-signal>
  <files>.planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md</files>
  <action>The trusted orchestrator enforces the initial blocking Scope contract before drafting an artifact or dispatching integration. Graph ready/branched is insufficient. Keep missing parent work parked with its ID and evidence; prepare the existing phase integrator against the complete epic snapshot without changing carrier branch code.</action>
  <verify><human-check>Verify seven merged PRs, seven ancestry checks, unchanged epic SHA and the actual integrator snapshot identity.</human-check></verify>
  <done>All seven parents are proven in the actual integration input; incomplete inputs remain blocked without phase-40 code.</done>
</task>
<task type="auto">
  <name>Assemble immutable phase identity and cross-ticket evidence</name>
  <files>.planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md</files>
  <action>After the initial checkpoint proves all seven terminal tickets are merged into the pinned epic snapshot, record exact head/tree/base, ticket set/digest, focused verification receipts, installed host/hook/policy identities, first-response and wake observations, candidate replay/no-executor evidence, local projection count/bytes and usage/quality cohorts. Preserve unknown attribution and failed/parked/recovery rows. Draft the integration evidence under the existing host flow; do not label an offline fixture as live installation.</action>
  <verify><automated>node -e 'const fs=require("node:fs");const p=".planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md";const s=fs.readFileSync(p,"utf8");for(const x of ["Head","Base","T-41-01","T-41-06","T-41-08","REQ-151","REQ-156","REQ-157","Verdict"])if(!s.includes(x))process.exitCode=1'</automated></verify>
  <done>The draft names all seven ticket outcomes and combined identity, with usage and quality limitations stated explicitly.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Seal independent integration verdict and approve final handoff</name>
  <what-built>Sealed phase-41 INTEGRATION.md with combined identity, seven requirement outcomes and an independent integrator verdict.</what-built>
  <how-to-verify>Review ticket set/head/base, blocking findings, installed behavior, signed recovery, matched usage/quality readiness and the explicit savings verdict; approve handoff only when functional gates pass.</how-to-verify>
  <resume-signal>Record explicit phase handoff approval or blocking findings.</resume-signal>
  <files>.planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md</files>
  <action>Invoke the existing independent integrator against the authenticated combined phase snapshot and seal its full result. Resolve blocking findings through scoped follow-up tickets before a passed functional verdict. Present the sealed outcome, signing/recovery and installed observations to the human reviewer. Human approval is required for this high-risk release handoff; an inconclusive savings verdict is acceptable only with verified functional and quality gates.</action>
  <verify><automated>node -e 'const fs=require("node:fs");const s=fs.readFileSync(".planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md","utf8");if(!/Verdict|verdict/.test(s)||!/ticket.set|ticket_set/i.test(s)||!/finding/i.test(s))process.exit(1)'</automated><human-check>Review sealed integrator verdict, candidate recovery gate, installed behavior and quality/readiness evidence before merge.</human-check></verify>
  <done>A sealed independent integration artifact records a passed functional verdict or blocking findings, and the human checkpoint records release approval only when gates pass.</done>
</task>
</tasks>

## Acceptance criteria

- REQ-151–156/P41-A–F are individually mapped to integrated artifacts and observed behaviors; no phase-40 code is assumed.
- Independent checker/integrator judgments, signed finalization, installation identity and merge gates remain intact. Savings verdict is inconclusive when readiness evidence is incomplete, while verified functional fixes remain eligible to ship.
- High-risk final handoff has an actual human checkpoint. A separate initial checkpoint rejects early graph-ready status until all seven merged-parent ancestry proofs and the complete integrator snapshot are present.

## Test strategy

Use the focused commands in T-41-01–06, T-41-08 and their CI receipts. The integrator reads the combined epic state and produces `INTEGRATION.md`; live installed observations and matched cohort measurements are manual/CI acceptance evidence, not offline commands.

## Verification commands

- `node -e 'const fs=require("node:fs");const s=fs.readFileSync(".planning/phases/41-reduce-pipeline-subscription-overhead/INTEGRATION.md","utf8");for(const x of ["T-41-01","T-41-02","T-41-03","T-41-04","T-41-05","T-41-06","T-41-08","REQ-151","REQ-156","REQ-157"])if(!s.includes(x))process.exitCode=1'`

<threat_model>
| Threat | Severity | Disposition | Mitigation |
| --- | --- | --- | --- |
| Incomplete ticket set or forged outcome is released | high | mitigate | Bind epic head/base/ticket digest to independently sealed integrator result and trusted receipts. |
| Missing live usage is misreported as savings | medium | mitigate | Enforce existing readiness and quality gates; allow only an explicit inconclusive verdict. |
</threat_model>

<verification>Check integration artifact structure, sealed identity and human review record; run CI quality gates through the existing delivery host.</verification>
<success_criteria>Phase 41 can be handed off on a verified functional verdict with honest installed, usage and quality evidence.</success_criteria>
