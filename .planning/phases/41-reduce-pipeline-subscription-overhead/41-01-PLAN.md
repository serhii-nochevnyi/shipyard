---
phase: 41
plan: 01
title: "Measure bounded judgment launches and preserve governing references"
type: implementation
wave: 1
depends_on: []
files_modified:
  - plugins/delivery-pipeline/scripts/claude-role-host.cjs
  - tests/unit/claude-role-host.test.cjs
requirements: [REQ-151]
autonomous: true
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
delivery:
  ticket: T-41-01
  risk: medium
  human_checkpoint: false
must_haves:
  truths:
    - "REQ-151: each targeted judgment launch exposes complete assembled prompt bytes separately from packet bytes/4 estimate and observed first-response usage or an explicit unknown."
    - "REQ-151: arch-review, integrator, and sentinel keep selected backlog and governing transitive references without duplicating source content."
  artifacts:
    - plugins/delivery-pipeline/scripts/claude-role-host.cjs
    - tests/unit/claude-role-host.test.cjs
  key_links:
    - "selected source refs -> authenticated packet -> complete prompt -> dispatch identity -> supported first-response usage"
---

## Goal

REQ-151 yields an auditable role launch with bounded, nonduplicated input and a first-response measurement linked to its authenticated dispatch. This is the phase tracer: one arch-review fixture traverses source selection, packet validation, prompt admission, dispatch and a supported usage observation before the remaining roles expand from it.

## Context (Reads)

- `.planning/architecture/ADR-019-pipeline-subscription-efficiency.md:29-48`; `41-RESEARCH.md:203-246` and P41-A in `CONTEXT.md`.
- `plugins/delivery-pipeline/scripts/claude-role-host.cjs:265-330,677-728,1114-1163` already selects source/backlog refs and launches the role; `context-packet.cjs:397-475` validates selected inventory and packet size; `usage-report.cjs:145-215` matches supported transcript identities. Extend these seams.
- `tests/unit/claude-role-host.test.cjs:29-88,136-198` supplies repository, role launch and observed-result fixtures.
- T-39-17 is merged in release 0.63.0; `.planning/graph/delivery-state.json` may still show its stale local pending snapshot. Installed file identity is a separate observation.

## Scope

- Keep T-39-17 selection and bounds. Fix the demonstrated `reference_content` repetition between `role_context` JSON and the trusted prompt segment; retain a single authoritative instruction/reference copy and the packet's digest inventory. Keep mandatory source admission and named remedy before launch.
- Record dispatch/run/role, source revision, host/install and policy digests, selected refs and backlog IDs, packet bytes and bytes/4 **estimate**, complete prompt bytes, model/effort and supported first-response provider input/cache-read/cache-creation/output. Missing transcript match remains unknown. Do not equate the packet estimate with provider launch input.
- A failing partial-supersession fixture must demonstrate whether `architectureRefs` drops a still-governing constraint or transitive `DECISIONS.md`; correct that exact loss, preserve decision-level supersession and source bounds. If no loss reproduces, retain current selection and the regression fixture.

## Out of scope

No new packet selection system, model rung change, phase-40 host provenance system, or quota savings claim.

<tasks>
<task type="tracer" tdd="true">
  <name>Trace one arch-review launch through packet, prompt and first response</name>
  <files>plugins/delivery-pipeline/scripts/claude-role-host.cjs, tests/unit/claude-role-host.test.cjs</files>
  <behavior>REQ-151: a selected arch-review fixture launches once with source content once, a bounded complete prompt, packet bytes/4 labeled estimate, and provider counters attached only when a supported dispatch/transcript identity matches; absent match is unknown.</behavior>
  <action>RED: add the fixture and prove it fails. GREEN: remove the duplicated reference segment while preserving trusted instructions, add complete-prompt and installed identity metadata at the existing host launch/result seam, and correlate observed first-response usage by authenticated dispatch identity. REFACTOR: centralize the role measurement without changing policy or packet selection. Check the complete prompt bound before dispatch and retain the named mandatory-input remedy.</action>
  <verify><automated>node tests/unit/claude-role-host.test.cjs</automated></verify>
  <done>One arch-review request completes the selected packet to response path; counters are observed or unknown, never inferred from bytes/4.</done>
</task>
<task type="auto" tdd="true">
  <name>Expand the proven path to integrator and sentinel, and test governing references</name>
  <files>plugins/delivery-pipeline/scripts/claude-role-host.cjs, tests/unit/claude-role-host.test.cjs</files>
  <behavior>REQ-151: all three roles pass explicit selected IDs and bounds; partial supersession retains still-governing and transitive decisions; oversized required input refuses with a remedy before any launch.</behavior>
  <action>RED: add fixtures for all targeted roles, empty and nonempty selected backlog, required-source overflow, and a partially superseded ADR with transitive decisions. GREEN: apply the traced measurement to each role and change `architectureRefs` only if the fixture proves a lost governing constraint. REFACTOR: share a single reference inclusion rule and keep selected inventory separate from full backlog. Preserve the independent judgment schema and result sealer.</action>
  <verify><automated>node tests/unit/claude-role-host.test.cjs</automated></verify>
  <done>All three role fixtures pass; the launch record distinguishes source identity, packet estimate, complete prompt size and observed provider counters.</done>
</task>
</tasks>

## Acceptance criteria

- REQ-151/P41-A fixtures prove selected source/backlog, no duplicated reference text, required input admission and partial supersession semantics for all three roles.
- A release operator records installed host, packet builder and policy hashes plus an actual first response for each role; source hash equality alone is not a behavioral claim. Compare only matched runtime/model/effort/role cohorts.

## Test strategy

Each task follows RED → GREEN → REFACTOR with the existing hermetic role-host fixture. Installed role observations and provider usage matching belong to the release evidence, not to an offline executor command.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/claude-role-host.cjs`
- `node tests/unit/claude-role-host.test.cjs`

<threat_model>
| Threat | Severity | Disposition | Mitigation |
| --- | --- | --- | --- |
| Prompt data masquerades as role instructions | high | mitigate | Keep trusted instruction outside the evidence packet and validate packet source digests before dispatch. |
| Oversized or stale source input bypasses admission | medium | mitigate | Preserve packet and complete-prompt bounds and named prelaunch refusal. |
</threat_model>

<verification>Run the scoped commands above. At installation, capture digests and supported first-response records with dispatch IDs.</verification>
<success_criteria>REQ-151 is implemented in source and fixture-tested; installed and measured status remains separately evidenced.</success_criteria>
