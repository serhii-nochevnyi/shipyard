---
phase: 29
plan: 06
title: "The acting half: resolve by target status, transition by id, never block"
type: implementation
wave: 5
depends_on: [T-29-05]
files_modified:
  - plugins/delivery-pipeline/commands/deliver.md
requirements: [REQ-65]
delivery:
  ticket: T-29-06
  risk: high
  human_checkpoint: true
  preauthorized: true
---

## Goal

The instructions that turn a work list into a tracker mutation. This is the
first place in this repository that tells an agent to WRITE to an external
system, which is why it is a checkpoint even though the empty-map default means
it cannot fire unconfigured.

## Context (Reads)

- ADR-008 D4, and the Context section's three schema findings — each of them
  would have shipped as a bug.
- `plugins/delivery-pipeline/commands/decompose.md:292-345` — Step 5, the
  existing tracker section: the tool list, the "skip with a one-line note when
  no MCP is connected" rule, and "a Jira error never blocks or fails
  decomposition". This ticket extends that posture to delivery unchanged.
- CLAUDE.md, "The Codex generator rewrites the word Claude in prose" — this file
  is the Codex skill's SOURCE, so any sentence whose meaning depends on naming
  Claude as a runtime distinct from the reader's INVERTS on the far side. Name
  the mechanism (`the connected tracker MCP`), never the runtime.

## Scope

A subsection of `deliver.md` stating, for each item `plan` emitted:

1. Ask the connected tracker MCP for the issue's available transitions.
2. Match the one whose **target status name** equals `target_status`. **Never
   match on the transition's own name** — the MCP schema says in as many words
   that *"Done does not match a transition named 'Review->Done'"*.
   **Write that rule as prose and do NOT write the banned identifier into the
   file**: T-29-01's pin sweeps this very path, so an executor who copies the
   identifier out of this plan turns their own PR red. Describe it; never spell
   it.
3. Transition by that **id**. Two connected MCP variants require different
   shapes — one takes `transition: {id}` and accepts no name, the other takes
   `transitionId` or `transitionName` — so the id is the only argument that
   works on both.
4. Record it: `jira-project.cjs record <ticket> <key> --transition-id <id> --status <name>`.
5. Target not reachable from the current status → `record --unreachable` naming
   the transitions that WERE offered. Jira workflows forbid arbitrary jumps, so
   this is an ordinary outcome, not a failure.
6. **Never pass a comment through `update`** — the schema says it may be dropped
   with the transition still reporting success, so it would be a silent lie in
   the audit trail.
7. No tracker MCP connected, or `jira_transitions` empty → skip with one line.
   Never an error, never a retry loop, and **never a reason to hold a merge**.

## Out of scope

- WHERE in the loop this runs, and the stop-gate/tick-rate statement — T-29-07.
- Comments, worklogs, the phase Epic, and `escalation` → Blocked. ADR-008 puts
  all four out of scope with a reason each.

## Acceptance criteria

- `deliver.md` contains the seven steps above, and `transitionName` appears
  nowhere in it (T-29-01's pin is the mechanical check; run it).
- The words "never blocks a merge" appear as a rule, not as an aside.
- **Runtime-inversion check, performed and pasted**: generate the Codex skill
  and re-read its `deliver` SKILL.md for this subsection. Confirm no sentence
  inverted. The generator is silent about what it changed, and this repo has
  shipped an inverted sentence before.
  **Generate into a throwaway HOME — never by running the installer.** Copy the
  isolation from `tests/smoke/codex-shipyard-smoke.sh:22-25` (`WORK=$(mktemp -d)`,
  `export HOME="$WORK"`, trap-cleanup). `make install-shipyard-codex` reaches
  `scripts/install-shipyard-codex.sh:318`, which runs
  `gsd-tune --global --runtime codex --apply` and rewrites `runtime` in the
  operator's real `~/.gsd/defaults.json` — a ticket that silently reconfigures
  the host it was verified on has done more than its scope.
- Name honestly what CI cannot prove here: there is no live tracker in this
  repository, so this ticket's evidence is the pin, the generated-file re-read,
  and nothing more. Do NOT claim a behavioural verification that did not happen.
- **This ticket carries NO witnessed mutation of its own, and that is a decision
  rather than an omission.** ADR-006 requires one from every ticket that ships a
  GUARD; this one ships prose into a command doc and ships no guard. Its guard
  was landed separately and deliberately earlier, as T-29-01, so the mutation
  that proves this ticket's rule was already witnessed there. Say this in the PR
  body — a reviewer counting criteria will otherwise read zero as a miss, and
  the honest answer is better than a criterion invented to fill the column.

## Test strategy

`make test-fast` for the pin; the generator re-read by hand. There is no live
tracker to test against and the plan says so rather than implying otherwise.

## Verification commands

```bash
node --test tests/unit/source-contract.test.cjs
./tests/smoke/docs-smoke.sh
```
