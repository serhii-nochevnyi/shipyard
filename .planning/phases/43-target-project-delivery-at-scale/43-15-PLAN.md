---
phase: 43
plan: 15
title: "deliver-dispatch builds research and decomposition requests from the investigation directory"
type: implementation
wave: 11
depends_on: [T-43-12, T-43-14, T-40-15, T-40-25]
files_modified:
  - plugins/delivery-pipeline/scripts/deliver-dispatch.cjs
  - tests/unit/deliver-dispatch.test.cjs
  - plugins/delivery-pipeline/commands/investigate.md
  - plugins/delivery-pipeline/commands/decompose.md
  - tests/unit/planning-builders-contract.test.cjs
requirements: [REQ-172, REQ-175]
delivery:
  ticket: T-43-15
  risk: medium
  human_checkpoint: false
---

## Goal

D-14 (REQ-172), planning roles. The research and decomposition requests were also hand-built. `deliver-dispatch.cjs` builds them from the graph and the investigation directory, each round-tripping through the host's exported validator, with Codex parity or a named reason.

Same-phase parents: T-43-12 (primary by depth; `decompose.md`, shared `files_modified`; T-43-11, which edits `decompose.md` first, is its ancestor) and T-43-14 (`deliver-dispatch.cjs` and its test, shared `files_modified`, plus the `build <role>` entry this ticket extends).

## Context (Reads)

- `plugins/delivery-pipeline/scripts/deliver-dispatch.cjs` after T-43-14: the `build <role>` entry and the validator round-trip pattern.
- `plugins/delivery-pipeline/scripts/claude-delivery-host.cjs:18-19` `WORKFLOWS` includes `investigation-research`.
- `plugins/delivery-pipeline/scripts/claude-decompose-host.cjs:258` exports `canonicalRequest`.
- `plugins/delivery-pipeline/scripts/codex-decompose-host.cjs:317-326` exports `requestValue`; `ROLES` `:18-19` maps `gsd-phase-researcher` → `{role:'research', sandbox:'read-only'}`.
- `.planning/investigations/<INV>/` layout: `PROBLEM.md`, `RESEARCH-CONTRACT.md`, `DECISIONS.md`, `research/`.
- `plugins/delivery-pipeline/commands/investigate.md` and `decompose.md` (T-40-25 pending owner; `decompose.md` also edited by T-43-11 and T-43-12 first).

## Scope

- `deliver-dispatch.cjs` `build research <INV-id> --line <name>` and `build decomposition <INV-id|ADR-id> --phase <N>`:
  - inputs from the investigation directory (paths plus digests of `PROBLEM.md`, `RESEARCH-CONTRACT.md`, `DECISIONS.md`) and the graph; never inlined content, never model or effort;
  - Claude: research → the `investigation-research` workflow request validated by `claude-delivery-host` `validateRequest`; decomposition → `claude-decompose-host.canonicalRequest`;
  - Codex: both via `codex-decompose-host.requestValue`;
  - an invalid investigation id or missing input file exits 2 naming it.
- `commands/investigate.md` and `commands/decompose.md`: replace the hand-built request steps with the build commands.
- `tests/unit/deliver-dispatch.test.cjs`: both roles × both runtimes round-trip through the real validators using a temp investigation directory; missing `PROBLEM.md` exits 2.
- NEW `tests/unit/planning-builders-contract.test.cjs`: both prose files name the build commands; each resolves.
- Comment policy on added lines.

## Out of scope

- The five-role list beyond research and decomposition (T-43-14 owns the other three).

## Acceptance criteria

- Research and decomposition requests are built deterministically and accepted by the real validators on both runtimes.
- The planning prose uses the builders.

## Test strategy (TDD: failing test first)

1. RED: add the cases and the contract test; on base the roles are unknown to `build`.
2. GREEN: implement the builders and prose.
3. Re-run both tests and the docs smoke.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/deliver-dispatch.cjs`
- `node tests/unit/deliver-dispatch.test.cjs`
- `node tests/unit/planning-builders-contract.test.cjs`
- `bash tests/smoke/docs-smoke.sh`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
