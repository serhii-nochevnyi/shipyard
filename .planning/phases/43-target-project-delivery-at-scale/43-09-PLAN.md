---
phase: 43
plan: 9
title: "state-sync lists PRs by ticket head and open state and skips tickets whose merge into a landed epic is recorded; --full re-derives"
type: implementation
wave: 7
depends_on: [T-40-03, T-40-19, T-40-27]
files_modified:
  - plugins/delivery-pipeline/scripts/state-sync.cjs
  - tests/unit/state-sync-listing.test.cjs
requirements: [REQ-173, REQ-175]
delivery:
  ticket: T-43-09
  risk: medium
  human_checkpoint: false
---

## Goal

D-15 (REQ-173). On the proving ground state-sync took a 128 s median over 25 syncs because it lists `--state all --limit <pr_fetch_limit>` per repository and re-derives every ticket. It must list PRs by ticket head and open state, and not re-derive tickets whose merge into a landed epic is recorded immutably; `--full` re-derives everything.

## Context (Reads)

- `plugins/delivery-pipeline/scripts/state-sync.cjs` (at 22ef429b): `:459` `gh pr list … --state all --limit <cfg.pr_fetch_limit>`; `:466-469` truncation fallback to `prsForBranch` (`:537`, `--state all --head <branch> --limit 50`); `:473` open-only review listing. The file contains a NUL byte: read it with node or `grep -a`. Re-read after T-40-03, T-40-19 and T-40-27 land (pending owners).
- `plugins/delivery-pipeline/scripts/pr-ledger.cjs` (T-40-03): the PR number recorded at creation (`<graphDir>/pr-ledger.json`, phase-40 D-31).
- `plugins/delivery-pipeline/scripts/pipeline-config.cjs:537` `pr_fetch_limit: 1000`.
- INV-007 DECISIONS F11: "immutably landed" = ledger entry plus merge SHA.

## Scope

- `state-sync.cjs`:
  - Per repository: one `gh pr list --state open --json <PR_FIELDS>` pass (bounded by `pr_fetch_limit`, truncation still falls back per branch).
  - For each ticket that is neither in the open list nor immutably landed: look up by the ledger PR number (`gh pr view <n> --json …`), else `gh pr list --state all --head <branch> --limit 50` as today's per-branch path.
  - Immutably landed: the ticket's previous state is `merged` with a recorded merge SHA, it has a ledger entry, and its epic's PR is recorded as merged into the integration branch. Such a ticket keeps its previous row and costs no `gh` call.
  - `--full`: today's full re-derivation (all states, every ticket).
  - The summary line reports `listed_open`, `looked_up`, `skipped_landed`.
- NEW `tests/unit/state-sync-listing.test.cjs` (stub `gh` on `PATH` counting calls; temp graph dir): a landed ticket produces 0 calls; an open ticket appears from the single open listing; a closed-unmerged ticket is found by ledger number; `--full` restores the all-state listing; a ticket merged into an epic whose PR is still open is re-derived (not skipped).
- Comment policy on added lines.

## Out of scope

- The gsd_sync warning ordering (T-43-10 edits this file after this ticket).
- `pr-ledger.cjs` itself (T-40-03).

## Acceptance criteria

- Landed tickets cost no GitHub calls; a normal sync lists only open PRs per repository.
- `--full` re-derives everything as today.

## Test strategy (TDD: failing test first)

1. RED: write the test; on base the landed ticket triggers the all-state listing.
2. GREEN: implement the open listing, the ledger lookup, the landed skip and `--full`.
3. Re-run the new test and the YAML guard.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/state-sync.cjs`
- `node tests/unit/state-sync-listing.test.cjs`
- `node tests/unit/state-sync-yaml.test.cjs`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
