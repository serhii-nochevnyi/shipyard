---
phase: 43
plan: 2
title: "Resolve the publish-gate base from the recorded ticket base and origin/HEAD; the pre-push hook passes the ticket"
type: implementation
wave: 1
depends_on: []
files_modified:
  - plugins/delivery-pipeline/scripts/publish-gate.cjs
  - scripts/shipyard-pre-push-gate.sh
  - tests/unit/publish-gate.test.cjs
  - tests/unit/pre-push-gate.test.cjs
requirements: [REQ-170, REQ-175]
delivery:
  ticket: T-43-02
  risk: medium
  human_checkpoint: false
---

## Goal

D-12 (REQ-170). On a target repository whose default branch is `master` or `develop`, `publish-gate.cjs` found no `origin/main`/`main` and exited 2, so the pre-push hook blocked every push and the operator wrapped it. The base must come from the ticket's recorded base, then the repository's `origin/HEAD`, before the `origin/main`/`main` fallback; the hook passes the real ticket for ticket branches; an unresolved base still refuses.

## Context (Reads)

- `plugins/delivery-pipeline/scripts/publish-gate.cjs` `baseFor` (at 22ef429b `:24-40`): candidates `requested, COMMENT_POLICY_BASE, SHIPYARD_COMMENT_BASE, origin/${GITHUB_BASE_REF}, 'origin/main', 'main'`; `:39` throws `cannot resolve a base ref in ${worktree}`; `:70` exits 2.
- `scripts/shipyard-pre-push-gate.sh:62`: passes the literal `--ticket publish`; it never passes a real ticket or base.
- `plugins/delivery-pipeline/scripts/state-sync.cjs` (at 22ef429b `:843-892`): the recorded base `s.base` per ticket in `delivery-state.json`.
- `plugins/delivery-pipeline/scripts/graph-dir.cjs resolveGraphDir`: the graph directory resolver.
- `scripts/install-shipyard-claude-hook.sh:143`: the hook bundle copies only literal `require('./x.cjs')` dependencies. Any new require in `publish-gate.cjs` must be literal and relative.
- `43-CONTEXT.md` P-05: project-root discovery.

## Scope

- `publish-gate.cjs`:
  - New options `--ticket <T-NN-NN>` (already accepted) and `--project-root <dir>`; env `SHIPYARD_PROJECT_ROOT`.
  - Project root (P-05): `--project-root`, else `SHIPYARD_PROJECT_ROOT`, else the parent of `git -C <worktree> rev-parse --path-format=absolute --git-common-dir` (the pushing repository's main worktree).
  - Candidate order: `--base`, `COMMENT_POLICY_BASE`, `SHIPYARD_COMMENT_BASE`, `origin/${GITHUB_BASE_REF}`, the recorded base of the ticket (from `<projectRoot>/.planning/graph/delivery-state.json`, row `base`, tried as `origin/<base>` then `<base>`), the ref `refs/remotes/origin/HEAD` resolved with `git symbolic-ref --quiet`, then `origin/main`, `main`. Never add literal `master` or `develop` candidates.
  - A missing or unreadable delivery-state, or a ticket id not in it, skips that candidate silently (it is not evidence of anything); the next candidate is tried.
  - `--json` output gains `base_source` (`flag|env|recorded|origin-head|fallback`).
  - Nothing resolves: exit 2 as today with the message naming the candidates tried.
- `scripts/shipyard-pre-push-gate.sh`: derive the ticket from the pushed branch when it matches `ticket/T-NN-NN-…` (the validate-graph `branchFor` shape) and pass `--ticket <id>`; for any other branch keep passing `--ticket publish`. Pass nothing else new; the gate discovers the project root itself. Stay on `set -euo pipefail`.
- NEW `tests/unit/publish-gate.test.cjs` (hermetic: `os.tmpdir()`, own `GIT_CONFIG_GLOBAL`, `commit.gpgsign=false`, bare origin per fixture):
  - origin default `master`, no `main`: resolves `origin/master` via `origin/HEAD`, `base_source: origin-head`;
  - origin default `develop`: same;
  - a delivery-state row `{base: 'epic/43-x'}` with that branch on origin and `--ticket T-43-01`: resolves `origin/epic/43-x`, `base_source: recorded`;
  - a Shipyard-shaped fixture (`main`, no origin/HEAD set): resolves `origin/main`, `base_source: fallback`;
  - no candidate at all: exit 2;
  - `SHIPYARD_PROJECT_ROOT` wins over the main-worktree discovery.
- `tests/unit/pre-push-gate.test.cjs`: a push of `ticket/T-43-02-x` invokes the gate with `--ticket T-43-02`; a push of `feat/x` keeps `--ticket publish`.
- Comment policy on added lines.

## Out of scope

- Comment-marker configuration (T-43-03, which edits `publish-gate.cjs` after this ticket).
- `tests/smoke/claude-hook-smoke.sh` (T-40-05, T-40-21).
- Reinstalling the installed hook (operator release note).

## Acceptance criteria

- `master`/`develop` default-branch repositories resolve a base without a wrapper.
- The recorded ticket base wins over `origin/HEAD` for ticket branches.
- Shipyard still resolves `origin/main`; an unresolved base still exits 2.

## Test strategy (TDD: failing test first)

1. RED: write `tests/unit/publish-gate.test.cjs` and the new `pre-push-gate` cases; on base the `master`, `develop` and recorded cases fail (exit 2 / wrong base) and the hook passes `--ticket publish`.
2. GREEN: implement the resolver and the hook change.
3. Re-run both files.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/publish-gate.cjs`
- `bash -n scripts/shipyard-pre-push-gate.sh`
- `node tests/unit/publish-gate.test.cjs`
- `node tests/unit/pre-push-gate.test.cjs`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
