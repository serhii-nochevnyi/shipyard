---
phase: 43
plan: 11
title: "Bind pre-existing Jira issues by recorded key: never create, refuse an unknown key, unlabelled issues are transitioned and commented only"
type: implementation
wave: 7
depends_on: [T-40-25]
files_modified:
  - plugins/delivery-pipeline/scripts/jira-export.cjs
  - tests/unit/jira-export.test.cjs
  - plugins/delivery-pipeline/commands/decompose.md
  - tests/unit/jira-binding-contract.test.cjs
requirements: [REQ-164, REQ-175]
delivery:
  ticket: T-43-11
  risk: medium
  human_checkpoint: false
---

## Goal

D-06 (REQ-164). The export looks up issues by the shipyard label and creates on no match, so an epic with existing issues would get about 28 duplicates; the operator disabled Jira and moved statuses by hand. Decompose proposes the ticket-to-issue mapping, the human approves it with the ticket set at Gate 2, a recorded key is authoritative (lookup by key, never create, refuse an unknown key), and issues without the shipyard label are only transitioned and commented. Status transitions stay the phase-29 projection.

## Context (Reads)

- `plugins/delivery-pipeline/scripts/jira-export.cjs` (at 22ef429b): label JQL lookups `:102-124`; `resolveLookup` `:138-162` returns `{action:'create'}` on no match; `:223,252` `on_no_match: 'create'`; `:244` summary `${id}: ${title}`; `upsertJiraKey` `:285-326` writes `delivery.jira`; `planExport` never reads `t.jira`; `JIRA_KEY_RE` (existing).
- `plugins/delivery-pipeline/scripts/validate-graph.cjs:177`: `jira` passes through to `tickets.json`.
- `plugins/delivery-pipeline/commands/decompose.md` (at 22ef429b `:405-414,488-501` Gate 2; `:585-606` the Jira export steps the model executes). T-40-25 is the pending owner; re-read after it lands.

## Scope

- `jira-export.cjs` `planExport`: when a ticket row has `jira` (matching `JIRA_KEY_RE`), emit a step with `lookup: [{kind: 'key', key}]`, `on_no_match: 'refuse'`, and `labelled_only_fields: ['summary', 'description']` plus `unlabelled: 'transition-and-comment-only'`; never a create action for that ticket. Tickets without a key keep today's label lookup and create.
- `resolveLookup`: `kind: 'key'` resolves by key; a missing issue returns `{action: 'refuse', reason: 'unknown key <K>'}`; an issue without the shipyard label returns `{action: 'transition-comment'}`; with the label, today's update.
- `commands/decompose.md`:
  - A step that proposes the ticket → existing-issue mapping from the investigation's Jira input (keys named in `PROBLEM.md`/intake), and records each approved key with `jira-export.cjs record` (the existing `upsertJiraKey` path).
  - Gate 2 shows the mapping table beside the ticket set for approval.
  - The export steps say: a recorded key is looked up by key, never created, an unknown key stops the export with the refusal, and unlabelled issues are only transitioned and commented.
- `tests/unit/jira-export.test.cjs`: a keyed ticket yields a key lookup with `on_no_match: 'refuse'` and no create; unknown key → refuse; unlabelled issue → transition-comment; unkeyed ticket unchanged.
- NEW `tests/unit/jira-binding-contract.test.cjs`: `decompose.md` contains the mapping proposal, the Gate 2 mapping approval, and the four export rules; each named command resolves (`jira-export.cjs record`, `plan`).
- Comment policy on added lines.

## Out of scope

- Jira status transition mapping (phase-29 projection unchanged).
- `human_checkpoint` Gate 2 prose (T-43-12 edits `decompose.md` after this ticket).

## Acceptance criteria

- A recorded key is authoritative: no duplicate issues, an unknown key refuses, unlabelled issues keep their summary and description.
- Gate 2 shows the mapping for approval.

## Test strategy (TDD: failing test first)

1. RED: add the export cases and the new contract test; on base the keyed ticket still creates and the prose lacks the rules.
2. GREEN: implement the key lookup and the prose.
3. Re-run both tests and the docs smoke.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/jira-export.cjs`
- `node tests/unit/jira-export.test.cjs`
- `node tests/unit/jira-binding-contract.test.cjs`
- `bash tests/smoke/docs-smoke.sh`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
