---
phase: 38
plan: 08
title: "Connect Claude sentinel through authenticated round dispatch"
type: implementation
wave: 6
depends_on: [T-38-07]
files_modified:
  - plugins/delivery-pipeline/scripts/claude-role-host.cjs
  - plugins/delivery-pipeline/scripts/dispatch-record.cjs
  - plugins/delivery-pipeline/scripts/front.cjs
  - plugins/delivery-pipeline/commands/deliver.md
  - plugins/delivery-pipeline/references/pr-sentinel.md
  - docs/dispatch-record.md
  - tests/unit/claude-role-host.test.cjs
  - tests/unit/dispatch-record.test.cjs
  - tests/unit/front.test.cjs
  - tests/smoke/claude-delivery-roles-smoke.sh
requirements: [REQ-112, REQ-113, REQ-116, REQ-118, REQ-120]
delivery:
  ticket: T-38-08
  risk: high
  human_checkpoint: true
---

## Goal

Launch Claude's round-scoped `pr-sentinel` through the trusted host and keep its
single guard identity visible against every ticket it owns.

## Context (Reads)

- `plugins/delivery-pipeline/scripts/claude-role-host.cjs`
- `plugins/delivery-pipeline/scripts/dispatch-record.cjs`
- `plugins/delivery-pipeline/scripts/front.cjs`
- `plugins/delivery-pipeline/scripts/role-artifact.cjs`
- `plugins/delivery-pipeline/scripts/dispatch-boundary.cjs`
- `plugins/delivery-pipeline/scripts/run-controller.cjs`
- `plugins/delivery-pipeline/references/pr-sentinel.md`
- `plugins/delivery-pipeline/commands/deliver.md`
- `.planning/phases/33-reduce-orchestration-context-and-transfer-sessions-safely/33-08-PLAN.md`

## Scope

Extend the trusted Claude role host with the fixed `pr-sentinel` role. Bind its
run and dispatch identity to the exact guarded-ticket set, each member's live
PR head/base, and the validated round artifact. Resolve model and effort inside
the host and require exact-session Claude transcript evidence before accepting
the round result.

Add a durable round-aware dispatch projection. A single authenticated guard
launch must become visible for every ticket in its validated membership set;
the board counts that launch once by its actual launch identity. Marking the
round is atomic and idempotent. Clearing requires the same round dispatch
identity and removes only its members. A round receipt cannot be copied into
per-ticket records unless the host verifies the artifact's complete ticket set
and digest against the boundary receipt. Do not infer membership from a prompt,
caller-supplied list, or receipt label.

Fail closed on missing, duplicate, added, removed, stale, or contradictory
members; mismatched head/base snapshots; invalid or cross-session transcript
evidence; and stale clear operations. Refusal leaves prior records intact and
does not partially hide tickets. Preserve per-ticket dispatch behavior and
historic records.

Correct the documentation that currently attributes the membership bridge to
T-33-08. That ticket implements fenced session ownership; it does not implement
round-to-ticket dispatch reconciliation. Document the actual host and
round-aware mark/clear contract without promising support until validation
passes.

## Out of scope

- Changing model or effort policy.
- Reworking Claude architecture-review or integration roles, which belong to
  T-38-07.
- Changing deterministic sentinel merge, review, CI, or human gates.
- Jira projection or creating Jira tickets.

## Acceptance criteria

- `pr-sentinel` has a fixed executable Claude host path and crosses
  resolve → validate → launch → receipt with explicit model and effort.
- The host authenticates the complete guarded ticket set, round subject,
  dispatch receipt, and live member identities before recording ownership.
- One round mark exposes all and only validated members to `front` and the stop
  gate, preserves the returned launch identity, and counts as one in-flight
  guard regardless of member count.
- Mark and clear operations are atomic, idempotent for the same round identity,
  and reject stale or mismatched identities without changing current records.
- Missing or contradictory ticket-set membership, PR head/base, model, effort,
  session, artifact, or receipt evidence fails closed before an actionable
  result is accepted.
- Existing per-ticket marks, independent guards, usage records, and expiry
  behavior remain valid.
- Delivery and operator docs state that T-33-08 is session fencing and that
  round membership is implemented only by this ticket.
- Capability-only checks make no model calls. The runtime live smoke remains
  opt-in and runs only in a disposable worktree.

## Test strategy

Exercise one boundary receipt over a multi-ticket round and prove every member
is visible while `agentsInFlight` counts the guard once. Test exact round
artifact and digest binding, absent or forged receipts, missing and extra
members, stale PR snapshots, concurrent marks, replayed clear, and preservation
of newer dispatches. Verify no failure path writes a partial overlay or hides a
ticket. Run the existing host live smoke only by explicit opt-in.

## Verification commands

- `node --test tests/unit/claude-role-host.test.cjs tests/unit/dispatch-record.test.cjs tests/unit/front.test.cjs`
- `bash tests/smoke/claude-delivery-roles-smoke.sh --capability-only`
