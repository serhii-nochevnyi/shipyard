---
phase: 33-reduce-orchestration-context-and-transfer-sessions-safely
plan: "01"
title: "Deliver durable CI and review transition events"
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/delivery-pipeline/scripts/wait-events.cjs
  - plugins/delivery-pipeline/scripts/ci-wait.cjs
  - plugins/delivery-pipeline/commands/deliver.md
  - plugins/delivery-pipeline/scripts/stop-gate.cjs
  - tests/unit/wait-events.test.cjs
  - tests/unit/ci-wait.test.cjs
  - tests/unit/stop-gate.test.cjs
  - plugins/delivery-pipeline/scripts/state-sync.cjs
autonomous: true
requirements: [REQ-90]
delivery:
  ticket: T-33-01
  risk: medium
  human_checkpoint: false
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A waiting delivery run survives a process restart and requests model work once per semantic CI or review transition, while merge still checks live state."
    - "Failure or missing evidence cannot silently bypass a mandatory live gate or the ADR-014 dispatch boundary."
  artifacts:
    - path: plugins/delivery-pipeline/scripts/state-sync.cjs
      provides: "Published observation and state digest binding"
    - path: plugins/delivery-pipeline/scripts/wait-events.cjs
      provides: "wait-events.cjs behavior and caller contract for T-33-01"
    - path: plugins/delivery-pipeline/scripts/ci-wait.cjs
      provides: "ci-wait.cjs behavior and caller contract for T-33-01"
    - path: plugins/delivery-pipeline/commands/deliver.md
      provides: "deliver.md behavior and caller contract for T-33-01"
  key_links:
    - from: "plugins/delivery-pipeline/scripts/wait-events.cjs"
      to: "tests/unit/wait-events.test.cjs"
      via: "Real producer/consumer path exercised by the tracer test"
    - from: "plugins/delivery-pipeline/commands/deliver.md"
      to: "plugins/delivery-pipeline/scripts/wait-events.cjs"
      via: "Actual command or trusted host consumes the validated result; no unused helper"
---

<objective>
Deliver durable CI and review transition events.

Purpose: A waiting delivery run survives a process restart and requests model work once per semantic CI or review transition, while merge still checks live state.
Output: plugins/delivery-pipeline/scripts/wait-events.cjs, plugins/delivery-pipeline/scripts/ci-wait.cjs, plugins/delivery-pipeline/commands/deliver.md, tests/unit/wait-events.test.cjs, tests/unit/ci-wait.test.cjs.
Implement the applicable decisions in CONTEXT.md, preserving D-02, D-03 and D-09.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
@.shipyard/generated/gsd-delivery-rules/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-reduce-orchestration-context-and-transfer-sessions-safely/CONTEXT.md
@.planning/phases/33-reduce-orchestration-context-and-transfer-sessions-safely/33-RESEARCH.md
@.planning/architecture/ADR-011-ROLLOUT.md
@.planning/architecture/ADR-014-mandatory-runtime-model-ladder.md
@.planning/architecture/ADR-014-DATA-MODEL.md
@.planning/architecture/ADR-014-INTERFACES.md

Context reads (existing behavior to extend; numbered anchors are planning-baseline locators):
- plugins/delivery-pipeline/scripts/ci-wait.cjs:364
- plugins/delivery-pipeline/scripts/state-sync.cjs:1039
- plugins/delivery-pipeline/scripts/front.cjs
- plugins/delivery-pipeline/scripts/sentinel.cjs:810
- plugins/delivery-pipeline/scripts/lock.cjs:342
- plugins/delivery-pipeline/commands/deliver.md:2122
- tests/unit/ci-wait.test.cjs:1

<interfaces>
Reuse withLock/lockDirFor/writeAtomic, check-state classification and state-sync's last-published metadata. Introduce wait-events.cjs exports observe, pending, acknowledge and a CLI used by deliver; observations and clock/sleep are injected. Store versioned records beside ci-waits.json under the graph's existing lock.
</interfaces>
</context>

## Scope and dependency contract

Root ticket: reuse the landed Phase 32/36 implementation; no cross-phase graph dependency.
Only the exact frontmatter paths may change. Runtime data created in disposable test projects is not repository source scope.
All paths are repository-relative. Use the dependency-free assert harness and injected I/O; no package installation. Production behavior tasks start with a failing behavior assertion, then implementation and green verification. Preserve D-02/D-03 live gates and explicit runtime policy. Do not launch or mutate external services in offline tests. Commit with the canonical ticket ID; executor stops at the commit. Full-suite execution belongs to CI. Existing slower integration commands are not advertised as sub-60-second checks.

## Out of scope

Model-grid or escalation changes, installed-agent edits, external package installs, global runtime settings, phase-34 convergence work and unsupported automatic session launches. No widening to unrelated command cleanup.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Recover one CI transition through the real waiter and delivery consumer</name>
  <files>plugins/delivery-pipeline/scripts/wait-events.cjs, plugins/delivery-pipeline/scripts/ci-wait.cjs, plugins/delivery-pipeline/commands/deliver.md, tests/unit/wait-events.test.cjs</files>
  <read_first>plugins/delivery-pipeline/scripts/ci-wait.cjs:364; plugins/delivery-pipeline/scripts/state-sync.cjs:1039; plugins/delivery-pipeline/scripts/front.cjs; plugins/delivery-pipeline/scripts/sentinel.cjs:810; plugins/delivery-pipeline/scripts/lock.cjs:342; plugins/delivery-pipeline/commands/deliver.md:2122; tests/unit/ci-wait.test.cjs:1</read_first>
  <behavior>An unchanged pending CI sequence yields no model-work event; a pending-to-failed transition produces one stable action ID; termination after publish and before acknowledgment redelivers that ID.</behavior>
  <action>Per D-01/D-04, create the fake-clock process-restart tests first. Implement observe/pending/acknowledge with schema_version, run_id, ticket, repository, PR, head, semantic observation digest, transition sequence, next_wake_at, deadline, backoff, pending action and ack. Include sequence as well as digest so A→B→A is a new transition. Persist pending before returning to the foreground caller; record durable consumption keyed by action ID before scheduling, and acknowledge only after durable dispatch reservation or durable no-action decision. Replay links to the same dispatch identity and reconciles it instead of generating another. Wire deliver's actual waiter call to consume compact events and resync before deciding. Preserve current exit/refusal behavior and ci-waits stall history. Missing new store bootstraps; malformed/newer schema refuses action without erasing history.</action>
  <verify><automated>node tests/unit/wait-events.test.cjs</automated><fails_when>Nonzero exit, any failed behavior assertion, missing final harness tally, or zero executed assertions. Import/fixture errors are failures, never acceptable RED evidence.</fails_when></verify>
  <done>A real ci-wait child process and command-consumer fixture recover the same pending action after crash without a second logical dispatch; legacy stall counters survive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wake on reviews and actionable work with bounded recoverable waiting</name>
  <files>plugins/delivery-pipeline/scripts/wait-events.cjs, plugins/delivery-pipeline/scripts/ci-wait.cjs, tests/unit/wait-events.test.cjs, tests/unit/ci-wait.test.cjs, plugins/delivery-pipeline/commands/deliver.md</files>
  <read_first>plugins/delivery-pipeline/scripts/ci-wait.cjs:364; plugins/delivery-pipeline/scripts/state-sync.cjs:1039; plugins/delivery-pipeline/scripts/front.cjs; plugins/delivery-pipeline/scripts/sentinel.cjs:810; plugins/delivery-pipeline/scripts/lock.cjs:342; plugins/delivery-pipeline/commands/deliver.md:2122; tests/unit/ci-wait.test.cjs:1</read_first>
  <behavior>Review changes wake the run; reordering and timestamps do not; unreadable observations stay unknown; newly actionable work interrupts waiting; concurrent observers neither lose nor multiply transitions.</behavior>
  <action>Extend the first slice to checks and review facts using existing reviewer/check semantics, canonical sorted identities and states. Persist capped exponential delay based on the existing interval and deadline; reset on meaningful change and clip each sleep to remaining budget. Refresh the live check/review observation through the existing state-sync publisher at bounded wake intervals, then validate its published binding before comparison; rereading an unchanged local snapshot is not a live poll. Re-read actionable work/capacity and child status each wake. Test a review-only change arriving while CI remains pending and a stalled publisher, so cached state cannot suppress a required wake. Keep parent/child PR deduplication, existing empty-window parking and wake-lift rules. Outages do not increment stall counts; partial outage affects only unknown tickets. A timeout is a bounded terminal event, never success. Serialize concurrent observer/ack writes. No detached daemon or model turn per unchanged poll.</action>
  <verify><automated>node tests/unit/wait-events.test.cjs</automated><fails_when>Nonzero exit, any failed behavior assertion, missing final harness tally, or zero executed assertions. Import/fixture errors are failures, never acceptable RED evidence.</fails_when></verify>
  <done>Fake-time tests cover response reorder, ABA transitions, full/partial outage, deadline restart, duplicate acknowledgment, corrupt storage, two observer processes, and actionable work during wait.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Reject interrupted snapshot publication before waking work</name>
  <files>plugins/delivery-pipeline/scripts/state-sync.cjs, plugins/delivery-pipeline/scripts/wait-events.cjs, tests/unit/wait-events.test.cjs</files>
  <read_first>plugins/delivery-pipeline/scripts/state-sync.cjs:1039; plugins/delivery-pipeline/scripts/lock.cjs:342</read_first>
  <behavior>A process crash between state/front writes and final metadata publication cannot cause an event from a mixed snapshot; dispatch overlay refresh is distinguishable from a new observation.</behavior>
  <action>Per D-04, extend state-sync's last-published metadata with a versioned digest binding for its authoritative state payload and observation generation. Persist a canonical observation projection in metadata so a later dispatch overlay rewrite of front does not invalidate observation identity. Read metadata and state under the same publisher lock, verify the bound bytes, then derive observations; use current front only for scheduling eligibility. A legacy metadata record without binding forces a fresh sync before event publication, preserving old files and wait history. Do not infer a completed publication merely from matching timestamps or two identical reads of metadata. Inject process failure after each payload write and before metadata, then restart the real observer. Keep existing state/front consumer shapes and last-publish ordering; the next full sync repairs incomplete publication. Test concurrent state-sync versus observer and overlay-only front refresh.</action>
  <verify><automated>node tests/unit/wait-events.test.cjs</automated><fails_when>Nonzero exit, any failed behavior assertion, missing final harness tally, or zero executed assertions. Import/fixture errors are failures, never acceptable RED evidence.</fails_when></verify>
  <done>A mixed or legacy-unbound snapshot requests resynchronization without issuing work; a complete publication yields the intended semantic transition once, even after overlay refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Untrusted GitHub responses → local event store → orchestrator.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-33-01-T | Tampering | plugins/delivery-pipeline/scripts/wait-events.cjs | high | mitigate | Tracer validates identity and complete evidence at the consuming boundary; negative tests reject stale/forged/corrupt input. |
| T-33-01-E | Elevation of privilege | command/host acceptance | high | mitigate | D-02/D-03 preserve live mutation gates and authenticated runtime launch; data-only artifacts cannot grant authority. |
| T-33-01-D | Denial of service | bounded orchestration | medium | mitigate | Bound envelope/read/wait work and represent incomplete results explicitly; preserve full referenced evidence. |
| T-33-01-I | Information disclosure | stored artifacts and metadata | medium | mitigate | Contained paths and metadata-only counters/checkpoints; never collect credentials or ambient environment. |

Specific controls and risk rationale: Untrusted GitHub responses → local event store → orchestrator. Tampering/high: bind semantic identity and refuse corrupt snapshots. Denial-of-service/medium: persisted deadlines and bounded backoff. Elevation/high: event never substitutes for sentinel's live head/review/check gates.
No package install surface is added.
</threat_model>

## Test strategy

- An unchanged pending CI sequence yields no model-work event; a pending-to-failed transition produces one stable action ID; termination after publish and before acknowledgment redelivers that ID.
- Review changes wake the run; reordering and timestamps do not; unreadable observations stay unknown; newly actionable work interrupts waiting; concurrent observers neither lose nor multiply transitions.
The tracer is an end-to-end local fixture of the real command/host/consumer, not only a helper unit test. Create its test file as the first RED action using the existing harness. New slices use fake time and bounded subprocess fixtures, targeting less than 30 seconds. Record RED command/exit/assertion and GREEN evidence; missing import or syntax error is not valid RED. Negative tests match the intended refusal code/reason and assert that launch, publication, acknowledgment or verdict mutation did not occur; a generic thrown error is insufficient. Call the harness done() and assert at least one registered scenario so a no-op test cannot pass. Run each verification command from repository root. Existing integration suites can run longer and must not be substituted for quick checks.

## Verification commands

- `node tests/unit/wait-events.test.cjs`
- `node tests/unit/ci-wait.test.cjs`
- `node tests/unit/front.test.cjs`
- `node tests/unit/sentinel.test.cjs`
- `node tests/unit/stop-gate.test.cjs`

<verification>
A waiting delivery run survives a process restart and requests model work once per semantic CI or review transition, while merge still checks live state.
Run the commands above and retain their exit codes. Generated tests use disposable homes and do not imply a live automatic-transfer capability. CI owns the full suite.
<human-check>No merge checkpoint beyond normal gates. Compare OPT-06 alone before combined rollout; treatment may be disabled while retaining pending events and existing live gates.</human-check>
</verification>

## Delivery risk and checkpoint

No merge checkpoint beyond normal gates. Compare OPT-06 alone before combined rollout; treatment may be disabled while retaining pending events and existing live gates.
The frontmatter delivery checkpoint is the conveyor's merge gate, distinct from autonomous local implementation. No preauthorized approval is implied. Required internal review/fixes precede opening a PR that triggers external review. Executors do not publish.

<success_criteria>
- Mixed or legacy-unbound snapshots request resynchronization without issuing work; interrupted publication and overlay-refresh tests pass.
- A real ci-wait child process and command-consumer fixture recover the same pending action after crash without a second logical dispatch; legacy stall counters survive.
- Fake-time tests cover response reorder, ABA transitions, full/partial outage, deadline restart, duplicate acknowledgment, corrupt storage, two observer processes, and actionable work during wait.
- Every scoped verification command exits zero and each negative case fails closed.
- All modified source paths are declared; existing role/model and live-gate contracts survive.
</success_criteria>

<output>
Create `.planning/phases/33-reduce-orchestration-context-and-transfer-sessions-safely/33-01-SUMMARY.md` when done. Record command-backed RED/GREEN evidence, commits, treatment/capability limits, and any unresolved live verification. Do not claim a phase pass from ticket completion.
</output>
