---
phase: 40
plan: 25
title: "Update investigate and decompose prose for host state, line re-dispatch, Codex sealing and untracked planning"
type: implementation
wave: 6
depends_on: [T-40-12, T-40-17, T-39-10, T-39-11]
files_modified:
  - plugins/delivery-pipeline/commands/investigate.md
  - plugins/delivery-pipeline/commands/decompose.md
  - tests/unit/host-state-prose-contract.test.cjs
requirements: [REQ-149, REQ-146, REQ-147, REQ-148, REQ-142]
delivery:
  ticket: T-40-25
  risk: low
  human_checkpoint: false
---

## Goal

The investigate and decompose commands describe what the code now does:

- REQ-149e (D-14): both commands explain that `~/.local/state/shipyard/…` (for example `codex-decompose/`) is Shipyard's evidence store outside the model worktree. It is not a leak and not something to clean.
- REQ-146 (D-11): `investigate.md` explains that a failed research line is named with its cause, that the other lines stay sealed, and how to re-dispatch only that line.
- REQ-147/148 (D-12, D-13): both describe the sealed `shipyard.research-result.v1` / `shipyard.decomposition-result.v1` envelopes on Claude and Codex, and that the Codex researcher may write only its artifact.
- REQ-142 (D-16, D-17): `decompose.md` states that target projects keep `.planning/` untracked, names the one-time `planning-untrack.cjs` migration (dry run, then `--apply --confirm untrack-planning`), and inverts the REQ-133 warning for target projects: warn when `.planning/` is tracked. In the Shipyard repository it keeps its phase-39 meaning.

## Context (Reads)

- `plugins/delivery-pipeline/commands/investigate.md` (after T-39-11): `:35-38`, `:116-124` Codex research routing.
- `plugins/delivery-pipeline/commands/decompose.md` (after T-39-10): `:220-222` decomposition-result contract; the REQ-133 tracked-`.planning/` warning added by T-39-10.
- `plugins/delivery-pipeline/scripts/codex-decompose-host.cjs:73-90`: the host state directory.
- The new behaviour: `investigation-research.mjs` single-line mode (T-40-13), `codex-delivery-host.cjs` research consumer (T-40-12), `codex-decompose-host.cjs` containment and sealing (T-40-11), `planning-untrack.cjs` and `pr-hygiene.cjs applies` (T-40-17).
- `tests/unit/investigate-friction-contract.test.cjs` (T-39-11) and `tests/unit/decompose-friction-contract.test.cjs` (T-39-10): read them, do not edit them. The inverted warning must still satisfy their Shipyard-repo assertions. If a T-39-10 assertion demands the old direction unconditionally, stop and escalate rather than editing that test.
- 40-RESEARCH Pitfall 7.
- Existing implementation: prose only. I searched both commands for existing host-state explanations (`.local/state/shipyard`) and found only path mentions, no explanation.

- Graph position (amendment after user review): two same-phase parents. Primary parent T-40-12 (tie on depth with T-40-17, lowest id wins), whose chain carries T-40-13 and T-40-10 (the single-line re-dispatch CLI on both runtimes). T-40-17 (`planning-untrack.cjs`, which the contract test resolves) is a non-primary parent: this ticket is ready only once it has merged into the epic, and the worktree base-merges the epic before dispatch (T-40-27). T-40-11 is not a `depends_on`: its behaviour is described from D-12/D-13 and `40-11-PLAN.md`, the contract test names only scripts that already exist (`codex-decompose-host.cjs`), and the two land together through the epic.

## Scope

- `investigate.md`: add a short "Host evidence directory" paragraph, a "Recovering one failed research line" subsection with the exact single-line re-dispatch command for Claude and Codex, and the envelope sentence.
- `decompose.md`: the same host-state paragraph. The decomposition-result sentence covers both runtimes. A "Planning files in target projects" subsection names the untrack migration and states when the tracked/untracked warning fires for target projects and for the Shipyard repository.
- NEW `tests/unit/host-state-prose-contract.test.cjs`:
  - both files name `~/.local/state/shipyard` and describe it as outside the worktree;
  - `investigate.md` names the single-line re-dispatch and "stays failed until all four";
  - both name both envelope schemas;
  - `decompose.md` names `planning-untrack.cjs` with `--confirm` and states that the warning fires when `.planning/` is tracked in a target project;
  - every script path named exists.
- Comment policy: prose only.

## Out of scope

- `deliver.md` (T-40-24), README/Makefile (T-40-26), `docs/gsd_multilevel_delivery_pipeline.md` (T-39-11's file; unchanged here).

## Acceptance criteria

- The new contract test passes, and both phase-39 friction contract tests still pass unchanged.

## Test strategy (TDD: failing test first)

1. RED: write `tests/unit/host-state-prose-contract.test.cjs`. Run it; it fails on the missing paragraphs.
2. GREEN: edit both command files.
3. Re-run the new test and the two friction contract tests.

## Verification commands

- `node --check tests/unit/host-state-prose-contract.test.cjs`
- `node tests/unit/host-state-prose-contract.test.cjs`
- `node tests/unit/investigate-friction-contract.test.cjs`
- `node tests/unit/decompose-friction-contract.test.cjs`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
