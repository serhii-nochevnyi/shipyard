---
phase: 40
plan: 05
title: "Codex tuning writes no Claude-only keys and doctor reads the Codex agents manifest"
type: implementation
wave: 12
depends_on: [T-40-14, T-39-07, T-39-04]
files_modified:
  - plugins/delivery-pipeline/scripts/gsd-tune.cjs
  - tests/unit/gsd-tune.test.cjs
  - scripts/shipyard-doctor.cjs
  - tests/smoke/claude-hook-smoke.sh
requirements: [REQ-149]
delivery:
  ticket: T-40-05
  risk: low
  human_checkpoint: false
---

## Goal

Two REQ-149 Codex host-configuration point fixes (D-14):

- d. `gsd-tune --runtime codex` (report and `--apply`) proposes and writes no Claude-only keys. The `models.*` tuning rows are Claude-only (D-24).
- f. `make doctor` finds the Codex agents manifest where the installer writes it (`$CODEX_HOME/agents/.shipyard-manifest.json`) and reports the Codex bundle instead of skipping it.

## Context (Reads)

- `plugins/delivery-pipeline/scripts/gsd-tune.cjs:573-579`: the comment block and `GLOBAL_SAFE`, which claim `models.*` are tier aliases for both runtimes. `:581-612`: `TUNING_ALL`. At `:593`, `model_overrides.*` is gated on `runtime === 'claude'`, but at `:606-609` the `models.*` rows are ungated (anchor A19). 40-RESEARCH Pitfall 13.
- `tests/unit/gsd-tune.test.cjs`: the existing runtime cases (T-39-07 extended them).
- `scripts/shipyard-doctor.cjs:210-213` (anchor A25): looks for `<codexHome>/shipyard/manifest.json`.
- `scripts/install-shipyard-codex.sh:280` (anchor A26): `AGENT_MANIFEST_NAME=".shipyard-manifest.json"`, written into the agents directory. Read only here; the installer does not change.
- `tests/smoke/claude-hook-smoke.sh`: the doctor smoke (T-39-04 extended it).
- Existing implementation: the runtime gate already exists for `model_overrides.*` at `:593`. Reuse the same predicate for `models.*`. For the doctor, I searched for other manifest readers (`.shipyard-manifest.json`) and found only the installer and `tests/smoke/codex-shipyard-smoke.sh`, so the doctor reads the installer's constant name.

- Graph position (plan-check revision): depends on T-40-14 only to keep T-40-14 on the primary chain of T-40-22 (through T-40-21) and of T-40-11 and T-40-09 (through T-40-04). It is a linearization edge, not a code dependency.

## Scope

- `gsd-tune.cjs` (d, D-24): gate the four `models.*` rows on `runtime === 'claude'`, the same way `model_overrides.*` is gated. Remove `models.*` from the Codex-safe set. Update the existing comment block so it no longer claims both runtimes. Replace or delete the existing comment text; add no new comment lines. If a Codex project config already contains `models.*`, the Codex report lists those keys as ignored by Codex and never rewrites them.
- `tests/unit/gsd-tune.test.cjs`: `--runtime codex` report and `--apply` against a temp `.planning/config.json` write no `models.*` and no `model_overrides.*` key; `--runtime claude` still proposes both; a Codex config holding `models.research` is left byte-identical after `--apply`.
- `scripts/shipyard-doctor.cjs` (f): read `$CODEX_HOME/agents/.shipyard-manifest.json`. Report `ok` when it parses and lists agents that exist, `fail` when it is present but corrupt or names missing files, and `skip` only when it is absent. Stay read-only.
- `tests/smoke/claude-hook-smoke.sh`: populate a temp `CODEX_HOME` with an agents directory and a manifest, and assert the doctor output line reports the Codex bundle as present. Assert that a corrupt manifest yields a failing line.
- Comment policy on added lines.

## Out of scope

- Codex static variant sandboxes in `gsd-tune.cjs codexStaticVariants` (T-40-11).
- Doctor cache-vs-release checks (T-40-21).

## Acceptance criteria

- No Codex tuning path writes a `models.*` key; the Claude output is unchanged.
- The doctor reports the Codex manifest from the installer's real location, and the smoke asserts all three states (present, corrupt, absent).

## Test strategy (TDD: failing test first)

1. RED: add the Codex `models.*` cases to `tests/unit/gsd-tune.test.cjs` and the populated-manifest case to `tests/smoke/claude-hook-smoke.sh`. Run both and confirm they fail on assertions (keys written; manifest reported as skip).
2. GREEN: gate the rows and fix the doctor path.
3. Re-run both.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/gsd-tune.cjs`
- `node --check scripts/shipyard-doctor.cjs`
- `bash -n tests/smoke/claude-hook-smoke.sh`
- `node tests/unit/gsd-tune.test.cjs`
- `bash tests/smoke/claude-hook-smoke.sh`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
