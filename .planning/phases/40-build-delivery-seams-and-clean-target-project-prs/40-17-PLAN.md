---
phase: 40
plan: 17
title: "Add the publish-time PR hygiene gate and the confirmed one-time .planning untrack migration"
type: implementation
wave: 5
depends_on: [T-40-19]
files_modified:
  - plugins/delivery-pipeline/scripts/pr-hygiene.cjs
  - tests/unit/pr-hygiene.test.cjs
  - plugins/delivery-pipeline/scripts/planning-untrack.cjs
  - tests/unit/planning-untrack.test.cjs
requirements: [REQ-142]
delivery:
  ticket: T-40-17
  risk: medium
  human_checkpoint: false
---

## Goal

This is the core of D-07 for target projects.

- A deterministic gate, `pr-hygiene.cjs`, rejects a PR before publication when its title, body, head branch, commit subjects (D-19) or diff paths carry conveyor or GSD internals.
- A single applicability predicate exempts the Shipyard repository (D-26).
- `planning-untrack.cjs` is the explicit, confirmed, one-time migration that makes `.planning/` untracked in a target project (D-16). The conveyor never writes `.gitignore` on its own.

## Context (Reads)

- `plugins/delivery-pipeline/.claude-plugin/plugin.json:2` `"name": "shipyard"` (anchor A20): the exemption signal.
- INV-004 `research/pr-hygiene-evidence.md`: proving-ground #693 and #702 show `T-NN-NN:` titles, `Ticket:`, phase and ADR lines, `ticket/T-…` branches, and `.planning/` files in the epic → main diff.
- `plugins/delivery-pipeline/scripts/gate-trailer.cjs:369`: `gate_status:` in bodies. The gate rejects it in target projects, and T-40-19 moves the verdict to a commit status (D-20).
- `plugins/delivery-pipeline/scripts/validate-graph.cjs` branch grammar, `safeBranch` / `BRANCH_RE`: reuse the safe-ref grammar rather than a second one.
- 40-RESEARCH Code example 4 (applicability) and Runtime State Inventory (the untrack migration steps).
- Existing implementation: I searched `plugins/delivery-pipeline/scripts` for a PR text checker (`conventional`, `title`, `forbidden`, `hygiene`). `comment-policy.cjs` checks code comments only, and `role-artifact.cjs assertTicketMarker` requires the opposite. No PR text gate exists. For `.gitignore` or `git rm --cached` writers I searched `gsd-tune.cjs`, `adr-bootstrap.cjs` and `decompose.md`. None exists; the rejected alternative was automatic writes by bootstrap.

- Graph position (plan-check revision): depends on T-40-19 because the hygiene gate rejects `gate_status:` in bodies, which is only satisfiable once T-40-19 moves verdicts to the `merge-gate` status, and so T-40-19, T-40-03 and T-40-02 lie on the primary chain of T-40-22 and T-40-24.

## Scope

- NEW `pr-hygiene.cjs`:
  - `applies({root, ref})` per D-26. When `ref` is given, read the manifest with `git -C <root> show <ref>:plugins/delivery-pipeline/.claude-plugin/plugin.json`, a committed base that an agent cannot edit from its worktree. Without `ref`, read the file from `root`, which is only for trusted checkouts. It is fail-closed: any read or parse error means hygiene applies. A target-project worktree that adds a `shipyard` manifest does not become exempt when `ref` is the base.
  - `conventionalType(ticketType)`: the D-27 map (GSD ticket `type` → conventional type). T-40-18 and T-40-20 import it, so there is one map.
  - `NEUTRAL_PR_BODY_GUIDE`: the one-line body guide for target projects (summary, changes, tests; no ticket, phase, ADR, plan or tool names). T-40-15 passes it to executors through the existing `prBodyGuide` argument.
  - `NEUTRAL_DELIVERY_RULES_HINT`: the one-line rules hint for target projects (work only within files_modified; conventional-commit subjects with no ticket id). T-40-15 passes it through the existing `deliveryRulesHint` argument. Both strings pass `check` themselves.
  - `check({title, body, head, base, paths, commits, jiraKeys})` returns `{ok, violations: [{field, rule, excerpt}]}`:
    - title matches `^(feat|fix|docs|refactor|test|perf|build|ci|chore|revert)(\([a-z0-9-]+\))?!?: \S`, and the scope is not a ticket id;
    - head matches `<type>/<slug>` or `<type>/<JIRA-KEY>-<slug>` with the same type list (D-18 epic `feat/…` included);
    - title, body, head and every commit subject contain none of: `T-\d{2}-\d{2}`, `Ticket:`, `\bPhase \d+`, `ADR-\d+`, `PLAN.md`, `ticket/`, `epic/`, `.planning/`, `.shipyard/`, `gate_status:`, and the words `shipyard`, `gsd`, `conveyor` (case-insensitive, word-bounded);
    - no added, modified, copied or renamed path starts with `.planning/` or `.shipyard/`. Deleting such a path is allowed, because it removes internals: the one-time untrack migration below consists of those deletions and must pass this gate;
    - a Jira key is allowed in the head and body only when it is in `jiraKeys` from the graph.
  - CLI: `check --project-root <r> --base <b> --head <h> --title <t> --body-file <f> [--json]`. It collects `paths` with their status from `git diff --name-status <b>...<h>` and `commits` from `git log --format=%s <b>..<h>` using argument arrays. It exits 0 when `applies` is false (printing `exempt`), and exits 2 with the violations otherwise.
- NEW `planning-untrack.cjs`:
  - CLI `--project-root <r>` is a dry run that prints the planned `.gitignore` line and `git rm -r --cached .planning`.
  - `--apply --confirm untrack-planning` refuses when `applies` is false (Shipyard), the tree is dirty, the current branch is the default branch, or `.planning/` is already untracked. Otherwise it appends `/.planning/` to `.gitignore` if absent, runs `git rm -r --cached --quiet .planning`, and commits with the subject `chore: stop tracking local planning files` on the current branch, for a normal PR.
- NEW `tests/unit/pr-hygiene.test.cjs`:
  - each rule rejects a positive example and accepts a neutral one;
  - a Jira key allowed and not allowed;
  - `applies` is false for a temp root with a committed `shipyard` manifest, true for a root without one, true for a corrupt manifest, and true when the manifest exists only as an uncommitted worktree file and `ref` names the base;
  - the CLI over a hermetic git fixture in `os.tmpdir()` reports a commit subject `feat(T-40-03): x` and a `.planning/` path.
- NEW `tests/unit/planning-untrack.test.cjs` (hermetic git fixture): the dry run changes nothing; apply without `--confirm` refuses; apply on the default branch refuses; apply on a feature branch untracks and commits; the Shipyard manifest refuses; the migration branch passes `pr-hygiene.cjs check` (deletions of `.planning/` only), while a branch that adds a `.planning/` file does not.
- Comment policy on added lines.

## Out of scope

- Wiring the gate into publication, which is the deliver prose (T-40-24) and epic PRs (T-40-20).
- Executor PR body, commit subjects and branch generation (T-40-18, T-40-20).
- Inverting the REQ-133 warning (prose, T-40-25).

## Acceptance criteria

- A PR shaped like proving-ground #702 (`T-22-03: …` title, `Ticket: T-22-03` body, `ticket/T-22-03-x` head, `.planning/phases/…` path) produces one violation per leak.
- A clean `feat/add-batch-replace` PR with a conventional title passes.
- The Shipyard repository is always `exempt`.

## Test strategy (TDD: failing test first)

1. RED: write both test files. Run them; they fail because the modules are missing.
2. GREEN: implement `pr-hygiene.cjs`, then `planning-untrack.cjs`.
3. Re-run both.

## Verification commands

- `node --check plugins/delivery-pipeline/scripts/pr-hygiene.cjs`
- `node --check plugins/delivery-pipeline/scripts/planning-untrack.cjs`
- `node tests/unit/pr-hygiene.test.cjs`
- `node tests/unit/planning-untrack.test.cjs`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
