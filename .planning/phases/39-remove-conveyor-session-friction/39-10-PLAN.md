---
phase: 39
plan: 10
title: "Update decompose prose for bootstrap, hints, Jira plan and gates"
type: implementation
wave: 1
depends_on: []
files_modified:
  - plugins/delivery-pipeline/commands/decompose.md
  - tests/unit/decompose-friction-contract.test.cjs
requirements: [REQ-125, REQ-126, REQ-130, REQ-131, REQ-132, REQ-133]
delivery:
  ticket: T-39-10
  risk: medium
  human_checkpoint: false
---

## Goal

`/shipyard:decompose` prose bootstraps a missing GSD project from the ADR,
warns once on untracked `.planning/`, relays host hints in the user's language,
passes `--skip-ui` only on `UI design: none`, adopts the validator's
linearization condition, and exports Jira by executing the script plan. All
changes are pinned by a new text-only contract test (D-02, D-03, D-07, D-08,
D-09, D-10).

## Context (Reads)

- `plugins/delivery-pipeline/commands/decompose.md:60-66` (Step 0), `:80-94` (Step 0.5 gsd-tune), `:277-278` (`/gsd-plan-phase` invocation), `:329` (requirements from ROADMAP), `:425-432` (linearization), `:488-613` (Step 5 Jira)
- `tests/unit/source-contract.test.cjs:905-935,918,2002-2009` (pinned literals that must stay byte-identical; read only)
- `39-CONTEXT.md` fixed CLI contracts and hint wire format (`hint[<CODE>]: … — remedy: …`)
- `plugins/delivery-pipeline/scripts/graph-dir.cjs:74` (fallback resolution named in the untracked warning; read only)
- Existing-implementation check: the prose already has Step 0 intake, Step 0.5 tuning and Step 5 export; this ticket edits those steps in place rather than adding parallel sections. No contract test for these rules exists; `source-contract.test.cjs` is not extended to avoid a shared hot file.

## Scope

- Step 0 (before Step 0.5): when `.planning/config.json`, `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` is missing, run `node ${CLAUDE_PLUGIN_ROOT}/scripts/adr-bootstrap.cjs --adr <accepted ADR> [--phase <N>] --json` from the project root; it creates only missing files; exit 1 means the ADR has no decisions — relay the message and stop. Requirements come from the bootstrap report, never invented. `gsd-tune --apply` is allowed only for a `config.json` the bootstrap created in this run; otherwise Step 0.5 stays "do not apply tuning automatically".
- Step 0 untracked check: run `git ls-files --error-unmatch .planning >/dev/null 2>&1`; if `.planning/` is not tracked, tell the user once and never block: delivery worktrees are created from git, so they will not contain the PLAN files or graph and the conveyor falls back to `graph-dir.cjs` resolution; remedy: commit `.planning/`, or keep it untracked deliberately and pass the graph dir explicitly.
- Hint relay (any host step): on a non-zero host exit, read the `hint[<CODE>]` line and explain it to the user in their language with the remedy; never propose bypassing the host or switching runtime.
- Step 2: keep the literal `/gsd-plan-phase <N> --ingest .planning/.adr-ingest/*.ingest.md` byte-identical, append `[--skip-ui]`, and state the rule: add `--skip-ui` only when every selected ADR's header has the line `- **UI design:** none` (case-insensitive on `none`); otherwise invoke exactly as before. `adr-ingest.cjs` stays before `/gsd-plan-phase`.
- Linearization (`:425-432`): ordering without a code dependency is not a `depends_on`; linearize only if the child needs every parent's code at once; mention the Gate 2 warning "shares no files_modified" and that a real import dependency may legitimately trigger it.
- Step 5 Jira: replace the free-form MCP procedure with: run `node ${CLAUDE_PLUGIN_ROOT}/scripts/jira-export.cjs plan --repo <owner/repo> --project <KEY> --json`, execute each step verbatim through the connected Jira MCP (resolve the link type whose inward description reads `is blocked by`), call `jira-export.cjs record <T-NN-MM> <KEY>` for each created or found issue, then re-run `validate-graph.cjs`. Keep the existing skip rules and the sentence that a Jira failure never blocks decomposition.
- New `tests/unit/decompose-friction-contract.test.cjs` (text-only; reads `decompose.md`; no `fs.existsSync` on scripts).

## Out of scope

- The scripts themselves (T-39-01, T-39-05, T-39-06, T-39-09) and `investigate.md` (T-39-11).
- Editing `tests/unit/source-contract.test.cjs`; changing GSD's UI detection.

## Acceptance criteria

- The contract test asserts: `adr-bootstrap.cjs --adr` appears in Step 0 before the Step 0.5 `gsd-tune.cjs --check` line; the untracked warning names `git ls-files`, the worktree consequence, the remedy and "never block"; the hint relay names `hint[`, "user's language" and forbids bypassing; the pinned `/gsd-plan-phase … --ingest …` literal and `--skip-ui` are present and the condition names `UI design:** none`; the linearization text says an ordering-only relation is not a `depends_on`; Step 5 names `jira-export.cjs plan`, `jira-export.cjs record` and `is blocked by`, and keeps "never blocks".
- `node tests/unit/source-contract.test.cjs` stays green (all pinned literals intact).
- `bash tests/smoke/docs-smoke.sh` stays green.

## Test strategy (TDD: failing test first)

1. RED: write `tests/unit/decompose-friction-contract.test.cjs` with the assertions above; it fails on the current prose.
2. GREEN: edit `decompose.md` step by step, re-running the contract test and `source-contract.test.cjs` after each edit.
3. Script existence is checked at the phase gate, not here, so this ticket has no code dependency on the script tickets.

## Verification commands

- `node tests/unit/decompose-friction-contract.test.cjs`
- `node tests/unit/source-contract.test.cjs`
- `bash tests/smoke/docs-smoke.sh`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
