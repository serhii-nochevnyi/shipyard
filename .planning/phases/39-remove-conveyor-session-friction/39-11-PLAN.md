---
phase: 39
plan: 11
title: "Update investigate prose for Gate 1 ADR check and relaxed intake"
type: implementation
wave: 1
depends_on: []
files_modified:
  - plugins/delivery-pipeline/commands/investigate.md
  - docs/gsd_multilevel_delivery_pipeline.md
  - tests/unit/investigate-friction-contract.test.cjs
requirements: [REQ-125, REQ-126, REQ-129]
delivery:
  ticket: T-39-11
  risk: low
  human_checkpoint: false
---

## Goal

`/shipyard:investigate` needs only `.planning/investigations/`, Gate 1 builds
the ADR from `templates/adr/ADR.md` and refuses to close until
`adr-ingest.cjs --check` passes, and host hints are relayed in the user's
language. Pinned by a new text-only contract test (D-02, D-03, D-06).

## Context (Reads)

- `plugins/delivery-pipeline/commands/investigate.md:35-36` (research host), `:65` (`.planning/` precondition suggesting `/gsd-new-project`), `:170-184` (Gate 1; machine-read sections at `:176-179`)
- `docs/gsd_multilevel_delivery_pipeline.md:177` ("Preconditions: .planning/ is initialized (/gsd-new-project)")
- `tests/unit/source-contract.test.cjs:2011-2015` (pinned `each locked decision is one bullet under \`## Decision\`` and the line-broken `scope fences use\n     \`## Out of scope\``; read only)
- `39-CONTEXT.md` fixed CLI contracts and hint wire format
- Existing-implementation check: Gate 1 already runs `validate-inv.cjs`; this ticket adds the ADR check as the next step of the same gate rather than a new gate.

## Scope

- Step 1 precondition: replace the `.planning/`-must-exist rule with: `.planning/investigations/` is created if missing; no GSD project is required (decompose bootstraps it from the accepted ADR).
- Gate 1: generate the ADR from `${CLAUDE_PLUGIN_ROOT}/templates/adr/ADR.md`; add a step that runs `node ${CLAUDE_PLUGIN_ROOT}/scripts/adr-ingest.cjs --check --input <ADR path>` and must exit 0 before the INV is closed; on exit 1, fix the ADR and re-run. Keep the pinned literals byte-identical, including the line break.
- Hint relay: on a non-zero research-host exit, read the `hint[<CODE>]` line and explain it to the user in their language with the remedy; never propose bypassing the host or switching runtime.
- `docs/gsd_multilevel_delivery_pipeline.md:177`: investigate needs only `.planning/investigations/`; decompose bootstraps the GSD project from the ADR.
- New `tests/unit/investigate-friction-contract.test.cjs` (text-only; reads the two files; no `fs.existsSync` on scripts).

## Out of scope

- `adr-ingest.cjs`, the template and the bootstrap script (T-39-02, T-39-09); `decompose.md` (T-39-10).
- Re-validating existing accepted ADRs.

## Acceptance criteria

- The contract test asserts: `investigate.md` no longer requires `/gsd-new-project` and names `.planning/investigations/`; Gate 1 names `templates/adr/ADR.md` and `adr-ingest.cjs --check --input`, with the check before the close step; the hint relay names `hint[`, "user's language" and forbids bypassing; the docs file no longer states `/gsd-new-project` as the investigate precondition.
- `node tests/unit/source-contract.test.cjs` and `bash tests/smoke/docs-smoke.sh` stay green.

## Test strategy (TDD: failing test first)

1. RED: write `tests/unit/investigate-friction-contract.test.cjs`; it fails on the current prose.
2. GREEN: edit `investigate.md` and the docs line, re-running the contract test and `source-contract.test.cjs` after each edit.
3. Script and template existence is checked at the phase gate.

## Verification commands

- `node tests/unit/investigate-friction-contract.test.cjs`
- `node tests/unit/source-contract.test.cjs`
- `bash tests/smoke/docs-smoke.sh`
- `node plugins/delivery-pipeline/scripts/publish-gate.cjs --base origin/main --working-tree --json`
