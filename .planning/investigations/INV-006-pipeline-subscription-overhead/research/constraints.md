# INV-006 constraints — phase 41 sequencing and invariants

Source inspection at HEAD `68e3a158f9224709caa32665726355d4f4127b1b`; no tests, pipeline or raw logs. The static audit used `4830411b`; the Claude audit ends 2026-09-25 12:47 Kyiv (`docs/audits/2026-09-25-pipeline-subscription-efficiency.md:1-7`; `docs/audits/2026-09-25-claude-session-efficiency.md:1-13`; checked with `cat`). Installed behavior is unknown.

## Required source baselines and order

- **Current checkout:** `git rev-parse HEAD` returned `68e3a158f9224709caa32665726355d4f4127b1b`; `git rev-parse --git-common-dir` returned `/Volumes/KINGSTON/claude-shipyard/.git`. `node plugins/delivery-pipeline/scripts/model-policy.cjs fingerprint` returned `adr-014.v6` and `30e71fb4066fee5b67df14120532c0b4f8aedde169907bc16f70dd2569744968` (`plugins/delivery-pipeline/scripts/model-policy.cjs:50-63`). High for this checkout; recheck at implementation.
- **Phase 39 source:** `git rev-parse` returned T-39-17 tip `8ee1da0636702d75d50034af4982ac5418a5ad74` and local/remote epic tip `ce6544fcc661318f5a285fe8ae691d5dd9e3ea88`. `git merge-base --is-ancestor ticket/T-39-17-bound-role-packets-to-the-adrs-and-backl epic/39-remove-conveyor-session-friction` exited 0; against this HEAD it exited 1. Thus this worktree cannot serve as the packet implementation baseline. `.planning/phases/39-remove-conveyor-session-friction/39-17-SUMMARY.md:23-38` still says pending here and is not a live status check. T-39-17 retains ownership of selection/bounds (`39-17-PLAN.md:8-12,81-131`). High for local refs; main-branch integration and installation unknown.
- **Stop gate:** T-39-03 owns arming (`39-03-PLAN.md:8-14`); `rg -n 'isArmed|stop-gate-arm' plugins/delivery-pipeline/scripts/stop-gate.cjs` finds the implementation in this HEAD at `:206,500-516`. Its ticket tip is not an ancestor of epic/39 (same `git merge-base --is-ancestor` command, exit 1), which does not prove absence after squash integration. Installed-hook identity is unknown. Phase 41 first verifies installation and the six audit scenarios (`.planning/phases/41-reduce-pipeline-subscription-overhead/CONTEXT.md:84-98`).
- **Codex planning prerequisite:** `rg -n '^depends_on:|^files_modified:' .planning/phases/40-build-delivery-seams-and-clean-target-project-prs/40-{01,04,05,10,11,12,13,14}-PLAN.md` shows T-40-01 → 10 → 13 → 12 → 14, plus T-40-05 → 04 → 11 with 14 as a parent. T-40-10 owns the shared sealer, T-40-12 the Codex investigation consumer, T-40-11 contained writable research and sealed decomposition (`40-10-PLAN.md:20-66`; `40-12-PLAN.md:18-67`; `40-11-PLAN.md:23-70`). High for planned ownership, installation unknown.

**Sequencing verdict:** Phase 41 is intended between phases 39 and 40, but *local bootstrap alone does not establish* Codex Gate 1/2 investigate → ADR → decompose/check. `plugins/delivery-pipeline/scripts/codex-delivery-host.cjs:291-333` has no research-artifact consumer branch; `.planning/phases/41-reduce-pipeline-subscription-overhead/PLANNING-BLOCKERS.md:24-69` records missing sealer/decompose prerequisites. Before formal planning, deliver/install the governed prerequisite closure or revise phase order; recheck integration. This is a source-contract inference, not a failed live run.

## Hard technical constraints

- **Model floors:** ADR-014 `resolveDispatch` selects runtime-specific rungs. Codex research Sol/high (very-complex Sol/xhigh), executor Luna/max (critical Sol/high), arch-review/integrator Sol/high (critical Sol/xhigh), sentinel Luna/medium; Claude has separate rungs. Packet or turn reduction cannot lower these. Evidence: `nl -ba plugins/delivery-pipeline/scripts/model-policy-internal.cjs | sed -n '63,145p;575,612p'`; `.planning/architecture/ADR-014-INTERFACES.md:4-31`. High for source, installed application unknown.
- **Boundary ownership and replay:** Routed host/boundary owns resolution, launch and authenticated application receipt. Adapter-applied model/effort must match policy; an unknown application is not compliant. Duplicate `dispatch_id` refuses replay; repair escalation requires the immediately preceding authenticated ticket-bound receipt. Agent output cannot forge it. Evidence: `nl -ba plugins/delivery-pipeline/scripts/dispatch-boundary.cjs | sed -n '1331,1450p;1530,1558p;1860,1920p'`; `rg -n 'assertAgentReceiptIsNotForged|NONCOMPLIANT_RECEIPT' plugins/delivery-pipeline/scripts/role-artifact.cjs`; `ADR-014-INTERFACES.md:28-90`. High. T-40-22 keeps provenance in a sidecar, not the frozen receipt (`40-22-PLAN.md:25-50`).
- **Research input/output:** Each completed/blocked canonical line writes one complete contained file; trusted host checks digest, source revision, repository, policy and subject and seals a bounded `shipyard.role-artifact.v1` / `shipyard.research-result.v1` reference. Missing/duplicate/stale/tampered lines or forged receipts refuse; required context survives overflow. Evidence: host contract in task; `plugins/delivery-pipeline/commands/investigate.md:80-150`; `nl -ba plugins/delivery-pipeline/scripts/context-packet.cjs | sed -n '378,435p;510,565p'`; `40-10-PLAN.md:38-66`, `40-12-PLAN.md:30-67`. High as contract, installed support unknown.
- **Run/worktree fence:** `session-handoff` checkpoint needs snapshot, plan/ADR/policy digests, wait/action IDs, reservations/receipts, artifact refs, budgets, treatment and next action. Changed pinned bytes/head/base, unresolved launch or unknown active child prevents safe takeover; successor revalidates and acknowledges by compare-and-swap. Automatic transfer is unsupported at this source. Evidence: `nl -ba plugins/delivery-pipeline/scripts/session-handoff.cjs | sed -n '539,615p;752,765p;799,870p;899,941p'`. High.
- **Execution/finalization:** Current Codex host preflights canonical ticket/branch/base/head/signer and graph digest, requires a verified receipt, calls trusted finalizer and verifies the signed commit; host error marks failure. P41 recovery must persist an authenticated candidate binding repository/ticket/worktree, tree/base, plan/policy and verification, with idempotent finalization. Changed identity needs bounded revalidation or refusal; failed/pending gates remain visible. Candidate recovery is not present at this HEAD. Evidence: `nl -ba plugins/delivery-pipeline/scripts/codex-delivery-host.cjs | sed -n '165,251p;291,333p;367,455p'`; `nl -ba plugins/delivery-pipeline/scripts/delivery-commit-finalizer.cjs | sed -n '47,67p;108,175p'`; `CONTEXT.md:100-116`. High for current flow and declared target.
- **Packet and projection correctness:** T-39-17 packet selection must retain governing decisions, applicable constraints and transitive references; oversized mandatory input needs a named prelaunch remedy. Bytes/4 is only a packet estimate. Projection fingerprints may localize only by actual dependencies; `gsd-sync --check` stays read-only and detects relevant staleness and edge changes. Evidence: `.planning/phases/41-reduce-pipeline-subscription-overhead/CONTEXT.md:47-66,118-131`; `39-17-PLAN.md:81-131`; `nl -ba plugins/delivery-pipeline/scripts/gsd-sync.cjs | sed -n '1081,1131p;1155,1183p'`. High for contracts; observed savings unknown.

## Measurement and product invariants

The audit reports 861 deduplicated responses, 252,061,225 processed input tokens (245,666,703 cache reads), six false hook replies (5,097,592 input tokens), and a second T-39-16 executor pass (4,822,956 input tokens). These are **historical observed processing counters**; role attribution is inferred and some second-pass work may have been necessary. They are not unique text, quota, money or avoidable savings. Source checked with `sed -n '1,145p' docs/audits/2026-09-25-claude-session-efficiency.md` (lines 5-37,80-109); raw logs were not reread. High confidence in reported snapshot, low in causal savings.

For a savings claim, compare matched runtime/model/effort and role scope, one behavioral treatment at a time. Join parent/child usage to verified outcomes by authenticated identity; leave absent joins unknown. Deduplicate streams/replays; count cache categories, output, model turns, retries, failures, interruptions, parked/recovery work, handoff/startup/warmup and shell polls separately. `usage-report.cjs:293-329,653-671` leaves subscription usage and input per verified completion null. `orchestration-overhead.cjs:12-32,405-454` requires 20 completions, 95% attribution, comparable arms, seven-day defect window and no quality/recovery failure. Checked via `nl -ba` on these ranges; also `.planning/phases/41-reduce-pipeline-subscription-overhead/CONTEXT.md:133-152`. A functional fix can ship with an inconclusive efficiency result.

Independent planner/checker and integrator/architecture judgments remain required: the audit reports real findings (`docs/audits/2026-09-25-claude-session-efficiency.md:103-115`); `CONTEXT.md:167-173` excludes global model downgrades or bypassed gates. Source checked with `sed -n '1,145p'` on the audit and `nl -ba` on context. High for declared boundary; audit findings are historical.

## Phase ownership and integration interfaces

Owner paths and dependencies below were checked with `rg -n '^depends_on:|^files_modified:' -A9` on the cited plans:

- T-39-03: `stop-gate.cjs`, `stop-gate-arm.cjs`, `deliver.md` and tests (`39-03-PLAN.md:8-14`); P41-C starts with installed verification.
- T-39-17: `claude-role-host.cjs`, `context-packet.cjs` and tests (`39-17-PLAN.md:8-12,212-219`); P41-A starts from its integrated selection/bounds.
- T-40-08/11: Claude stream fixtures/tests; `codex-decompose-host.cjs`, `codex-dispatch-adapter.cjs`, `gsd-tune.cjs` and tests (`40-08-PLAN.md:8-14`; `40-11-PLAN.md:8-14`). P41 does not take stream compatibility or sandbox widening.
- T-40-10/12/14: `planning-result-sealer.cjs`, `codex-delivery-host.cjs`, in-flight `dispatch-record.cjs` and host tests (`40-10-PLAN.md:8-14`; `40-12-PLAN.md:8-10`; `40-14-PLAN.md:8-15`). Govern any prerequisite extraction explicitly.
- T-40-15/18/21/22: `deliver-dispatch.cjs`/`run-waker.cjs` request construction; `role-artifact.cjs`/`delivery-commit-finalizer.cjs` formatting; `host-provenance.cjs`, installers/doctor; `dispatch-record.cjs`, `claude-role-host.cjs`, `sentinel.cjs` provenance/merge rule (`40-{15,18,21,22}-PLAN.md:7-14`). P41-D may reuse the finalizer API; shared-file changes require explicit phase-40 ordering and no circular edge (`.planning/phases/41-reduce-pipeline-subscription-overhead/CONTEXT.md:154-165`).

**P41 ticket interfaces:** integrated phase-39 and installed digests; ADR-014 selection/receipt and repository/run/worktree; selected packet and assembled prompt paired with observed response; digest-pinned checkpoint and acknowledged successor; authenticated execution candidate bound to tree/base/graph/plan/policy/verification with idempotent finalization; projection dependencies/check; attributed usage, verified outcome and quality. These are required outputs, not existing artifacts (`CONTEXT.md:47-152`; `ADR-014-INTERFACES.md:4-90`; `codex-delivery-host.cjs:165-251`; `session-handoff.cjs:539-615`; `gsd-sync.cjs:1081-1183`; `orchestration-overhead.cjs:405-454`, checked with `nl -ba`).

## Unresolved checks for the investigation

- [ ] Is T-39-17 on the integration/default branch and in the installed role host? — owner: phase-39 release operator. Next: `git branch --contains 8ee1da0636702d75d50034af4982ac5418a5ad74`, PR/deploy record, installed digest, live selected packet.
- [ ] Is T-39-03 installed and does it suppress foreign/unchanged waits while allowing a relevant owner wake? — owner: host operator. Next: installed-hook digest and six audit regression scenarios.
- [ ] Which phase-40 planning prerequisite closure precedes Codex Gate 1/2, preserving ticket ownership and an acyclic graph? — owner: phase sequencing decision maker. Next: latest graph/PR state and sealed research/decomposition receipts.
- [ ] What authenticated P41-D candidate contract avoids changing T-40-18 formatting and T-40-21/22 provenance ownership? — owner: P41 planner and phase-40 owner. Next: design against `codex-delivery-host.cjs`, `delivery-commit-finalizer.cjs`, `role-artifact.cjs`; propose `$gsd-spike` if empirical validation is needed.
