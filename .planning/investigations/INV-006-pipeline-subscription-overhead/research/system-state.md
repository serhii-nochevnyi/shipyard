# INV-006 — system state at phase 41 source baseline

**Scope and evidence.** This is repository research at worktree HEAD `68e3a158f9224709caa32665726355d4f4127b1b`, not a runtime deployment test. `git rev-parse HEAD` returned that SHA; `git status --short` returned no changes before this artifact. `rg --files .planning/codebase` reported that no codebase map exists, so the paths below were read directly. The phase context is `.planning/phases/41-reduce-pipeline-subscription-overhead/CONTEXT.md:20-45,47-152`; the two required audits are `docs/audits/2026-09-25-claude-session-efficiency.md` and `docs/audits/2026-09-25-pipeline-subscription-efficiency.md`. Evidence commands for the precise source ranges are listed at the end. No pipeline, provider call, or test was run for this line.

## Measured baseline and its limit

The Claude audit's 2026-09-25 00:00–12:47 Kyiv snapshot deduplicated streamed/replayed response usage by stable message ID: 861 responses, 252,061,225 processed input tokens, of which 245,666,703 were cache reads, 6,392,800 cache creation, and 1,722 the uncached input field; output was 772,711. Main orchestrators were 137,968,609 processed input tokens (54.74%). Roles were inferred from task prefixes, not verified receipts. These are response processing counters, not unique text, provider billing, subscription credits, or a measured percentage of avoidable quota. Source: `docs/audits/2026-09-25-claude-session-efficiency.md:5-37`; checked with `nl -ba docs/audits/2026-09-25-claude-session-efficiency.md` (the audit describes its own JSONL and `usage-report.cjs` checks at lines 9-13). This research did not re-read private transcripts.

The stronger intervention signals in that audit are six false stop-hook replies processing 5,097,592 input tokens, six inspected judgment packets each selecting all 203 backlog sections (497,763 serialized backlog bytes), and a second T-39-16 executor session processing 4,822,956 input tokens after finalization/base recovery. The second pass may include necessary revalidation. The packet-only UTF-8 bytes/4 estimate undershoots observed first-response input because the launch has surrounding context: the audit pairs integrator estimates 340,318/356,878 with observed 587,880/617,209 input tokens. Source: `docs/audits/2026-09-25-claude-session-efficiency.md:41-70,80-97`; checked with `nl -ba ...` and `sed -n '72,112p' ... | nl -ba -v72`. The static audit uses older source revision `4830411b` and explicitly calls its figures estimates and proposals, so its defect descriptions need reconciliation with this HEAD (`docs/audits/2026-09-25-pipeline-subscription-efficiency.md:1-17,44-68`; checked with `nl -ba` on that file).

## Current entry points and flow

`plugins/delivery-pipeline/commands/investigate.md:29-49,80-150` selects one runtime, builds authenticated context packets for four research lines, routes them through the provider host and boundary, and requires sealed artifact references before synthesis. `plugins/delivery-pipeline/commands/decompose.md:281-345` takes an ADR into a researcher → planner → checker chain, requires three verified receipts and materialized plans, then applies Gate 2. `plugins/delivery-pipeline/commands/deliver.md:369-466,1226-1235` describes host-owned delivery dispatch, receipts, and Claude stop-gate arming. Source commands: `nl -ba plugins/delivery-pipeline/commands/investigate.md | sed -n '1,150p'`; `nl -ba plugins/delivery-pipeline/commands/decompose.md | sed -n '260,380p'`; `rg -n 'stage|execute|finaliz|sentinel|handoff|wait|host' plugins/delivery-pipeline/commands/deliver.md`; `nl -ba plugins/delivery-pipeline/commands/deliver.md | sed -n '1215,1235p'`. This maps contracts and call sites, not proof an installed copy follows them.

## P41-A — packet measurement and installed verification

At this HEAD, `claude-role-host.cjs:252-295` enumerates all architecture Markdown and passes their paths as required references; arch-review and integrator use that source set at `:372-385` and `:454-478`. `buildPacket` at `:598-616` supplies no explicit backlog selection, and `context-packet.cjs:237-285` then defaults to every non-closed/deferred/superseded backlog item, with both inventory and selected content. `makePrompt` at `claude-role-host.cjs:626-645` wraps the packet and only refuses above 1,500,000 assembled bytes; `PACKET_MAX_TOKENS` is 360,000 at `:24`, while `context-packet.cjs:445-493` records soft-ceiling overflow rather than refusing. The optional-reference branch at `:445-465` restores omitted content when omission would bring a packet below the ceiling. Commands: `rg -n 'selectedBacklogIds|backlogManifest|whySelected|selection|PACKET_MAX|PROMPT_MAX|architectureRefs' .../{claude-role-host,context-packet}.cjs`; `nl -ba`/`sed -n` for those exact ranges. This matches the audits at this HEAD, not necessarily a newer ticket branch or installed host.

T-39-17 owns the selection, deduplication and hard packet bounds: `.planning/phases/39-remove-conveyor-session-friction/39-17-PLAN.md:20-28,81-110,212-219`. `git log -8 --oneline` shows its planning commit (`5c670905`) in this HEAD, while `git merge-base --is-ancestor ticket/T-39-17-bound-role-packets-to-the-adrs-and-backl HEAD` exited 1. The source's absent `backlogInventory` and selection wiring independently confirms this HEAD does not contain that branch's implementation. Phase 41 therefore needs installed host/policy identity checks; complete assembled-prompt and first-response usage observation by comparable role/runtime/model/effort; fixtures plus runtime-path proof; and preservation of governing constraints, including partial supersession and transitive references. It must not implement T-39-17 again. Phase 41 context: `CONTEXT.md:47-66`; checked with `nl -ba`. Whether T-39-17 is integrated or installed later is **unknown**; check the integration branch and effective installed host before making a savings claim.

## P41-B — bounded parent continuation

`session-handoff.cjs:3-7,539-564` already has a Git-common-directory ownership store and a checkpoint requiring current snapshot, wait/action IDs, dispatch reservations/receipts, artifact refs, treatment, budgets and an exact next action. It checks pinned artifact digests and changed head/base at `:568-613`. Checkpoint, resume and acknowledgement are fenced by owner capability, unresolved launch state and live revalidation at `:799-869,899-941`; automatic transfer remains explicitly unsupported at `:752-759`. `run-waker.cjs:151-234` has wait readiness and a claim keyed by run/wake or state revision. Commands: `nl -ba plugins/delivery-pipeline/scripts/session-handoff.cjs | sed -n '1,125p;220,340p'`; `rg -n 'checkpoint|resume|acknowledge|next_action|artifact|pending|dispatch|in_flight' .../session-handoff.cjs`; `rg -n 'run_id|waiting|wake|claimed' .../run-waker.cjs`. Phase 41's residual is to use and measure a compact, durable phase/long-wait continuation, with accepted decisions, unresolved findings and gates, and to prove no duplicate/orphaned launch. Include checkpoint collection, successor startup and cache warmup in its treatment (`CONTEXT.md:68-82`; `orchestration-overhead.cjs:27,399-400`). A smaller parent transcript alone is not a measured saving.

## P41-C — wait wakeups and installed hook

The checked-in stop gate imports `isArmed` at `stop-gate.cjs:206`; on the unscoped path it allows an unarmed session at `:516`, while scoped runs choose the run's own front at `:500-514`. `stop-gate-arm.cjs:9-48` stores a per-session marker under the Git common directory; `deliver.md:1226-1235` arms Claude at cold start. `stop-gate.cjs:395-430` also records a block ledger and tests whether the board or qualifying journal evidence advanced. Commands: `nl -ba plugins/delivery-pipeline/scripts/stop-gate.cjs | sed -n '260,310p;445,485p;500,540p'`; `nl -ba plugins/delivery-pipeline/scripts/stop-gate-arm.cjs | sed -n '1,105p'`; `nl -ba plugins/delivery-pipeline/commands/deliver.md | sed -n '1215,1235p'`. T-39-03 owns the arming mechanism (`.planning/phases/39-remove-conveyor-session-friction/39-03-PLAN.md:18`; `CONTEXT.md:84-98`). Phase 41 must verify the *installed* hook identity and caller behavior, then address only a demonstrated residual wakeup gap. The six replies are baseline regression scenarios, not proof that this HEAD's hook is defective; the audit explicitly calls the installed-version diagnosis transcript evidence rather than a fresh reproduction (`claude-session-efficiency.md:80-86`). Shell polls and model replies need separate counts.

## P41-D — completed execution versus finalization

The Codex delivery host preflights canonical ticket graph, branch, base, head and signer (`codex-delivery-host.cjs:193-217`), launches the executor, then finalizes only after a verified dispatch receipt and nonempty worktree delta (`:220-251,291-331`). Its CLI run marks completion only for a committed artifact and marks host errors failed (`:432-454`). `delivery-commit-finalizer.cjs:47-67,108+` owns scoped status and the trusted commit; it is reusable rather than a new commit path. Commands: `nl -ba plugins/delivery-pipeline/scripts/codex-delivery-host.cjs | sed -n '165,265p;265,355p;395,455p'`; `nl -ba plugins/delivery-pipeline/scripts/delivery-commit-finalizer.cjs | sed -n '1,105p;300,365p'`; `rg -n 'function finalizeDeliveryCommit|function finalizedArtifact|function executorPreflight|candidate'` on those two files. There is no durable completed-execution candidate or resume-finalization branch in the inspected host flow: it goes from `launchAgent` to `finalizedArtifact` in one call. This is a source-flow finding, not a failed runtime test. Phase 41's residual is an authenticated, identity-bound candidate and idempotent recovery, with changed tree/base/plan/policy or verification evidence refusing stale reuse or triggering bounded revalidation (`CONTEXT.md:100-116`). The T-39-16 second pass is a scenario, not all proven waste. Phase 40 owns broader host provenance, request construction, schema/stream compatibility and finalizer formatting (`CONTEXT.md:154-165`); this recovery must reuse those contracts rather than claim the temporary local bootstrap makes phase 40 complete.

## P41-E — projection fingerprints

`gsd-sync.cjs:1081-1109` deliberately excludes volatile front heartbeat fields, then computes *one* source fingerprint from ROADMAP, project/config/graph state, all plan files, phase integration evidence and optional controller state. The same fingerprint is supplied to global STATE, REQUIREMENTS and ROADMAP and every generated summary/UAT/verification at `:1113-1125`. Its `--check` compares expected files byte-for-byte (`:1159-1165`), and publication skips identical files under the shared state lock (`:1168-1180,1211-1230`). Commands: `nl -ba plugins/delivery-pipeline/scripts/gsd-sync.cjs | sed -n '1060,1140p;1150,1195p;1200,1255p'`. The residual is dependency-specific fingerprints without losing relevant invalidation or the read-only consistency gate; compare changed generated-file count and diff bytes, not quota directly (`CONTEXT.md:118-131`).

## P41-F — outcome-linked measurement

`usage-attribution.cjs:4-17,27-53` already stores receipt/transcript correlation metadata without prompts or token totals. `usage-report.cjs:293-329,402-431` deduplicates Claude response fragments and separates input/cache/output; at `:653-671` it explicitly returns `subscription_usage: null` and `input_per_verified_completion: null` pending an outcome join. `orchestration-overhead.cjs:405-454` groups model turns, provider token coverage, attribution and quality, requires a comparable treatment and at least 20 completions with 95% dispatch attribution before promotion; insufficient evidence is inconclusive. `optimization-report.cjs:9-21` already names completed/failed/parked/interrupted run states and quality failures. Commands: `rg -n 'completion|attribut|usage|cache|p90|median|subscription|readiness' .../{usage-attribution,usage-report,orchestration-overhead,optimization-report}.cjs`; `nl -ba` on the named ranges. Phase 41 should join parent/child usage to verified delivery outcomes where identity permits; retain unknown attribution and recovery work; report cache categories, output, turns/retries, median/p90 per verified completion and total cohort, plus quality outcomes. Do not add iteration/thinking/cache subset counters twice or infer subscription credits (`CONTEXT.md:133-152`; `claude-session-efficiency.md:9-25`).

## Ownership and current unknowns

Phase 40's checked-in plans assign the shared planning sealer and Codex research consumer to T-40-10/11/12, and other provenance/host work to T-40-21/22 (`.planning/phases/40-build-delivery-seams-and-clean-target-project-prs/40-10-PLAN.md:20-47`, `40-11-PLAN.md:20-54`, `40-12-PLAN.md:20-45`; checked with `rg -n 'ownership|source revision|artifact|seal|consumer|stream'` over the named phase-40 plans). The phase-40 verification projection still labels T-40-08/11/15/18/21/22 pending (`40-build-delivery-seams-and-clean-target-project-prs-VERIFICATION.md:31-45`; checked with `nl -ba`). The current `codex-delivery-host.cjs:291-331` has no planning-artifact consumer branch; this worktree's research bridge may be temporarily patched outside this source, so its presence is **not** phase-40 completion. Check installed artifacts, integration refs and effective host identity before claiming deployment. `PLANNING-BLOCKERS.md:24-69` records the original phase-40 prerequisite conflict; it is a dated planning record rather than proof today's bootstrap is production-ready.

Open checks for the investigation: (1) exact integration and installed state of T-39-17 and T-39-03; (2) comparable first-response provider usage for the selected packet paths, including policy/host digests; (3) actual parent/child attribution and verified-completion coverage; (4) whether a bounded continuation can be adopted without an ambiguous active launch; (5) the minimal phase-41/phase-40 file ordering for finalization recovery. These are unknown until command-backed integration/installed observations or a controlled treatment exist. No subscription saving is established by this repository-only line.
