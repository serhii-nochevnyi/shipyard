# Research

Sources: the four research lines under `research/` (`system-state.md`,
`alternatives.md`, `constraints.md`, `risks.md`), all run through
`claude-investigation-host.cjs` at `8c264020` as `claude-opus-5-5/medium` with
verified receipts; the evidence files `research/session-evidence.md`,
`research/codex-flowpdf-evidence.md`, `research/pr-hygiene-evidence.md`; and
orchestrator checks run outside the research sandbox (noted "orchestrator").

## Current system state

- Product code at HEAD equals 0.61.0 (`befc970c`); `git diff --stat befc970c HEAD -- plugins scripts tests Makefile` is empty (system-state §0).
- Phase 39 progress (orchestrator, `gh pr list`, 2026-09-24 ~16:40 UTC): T-39-04 (#208) merged into the epic; T-39-05/06/07/09/10/11 PRs open (#209–#216); the epic → main PR #215 is open. Nothing of phase 39 is on `main` or in a release.
- The installed Claude cache `~/.claude/plugins/cache/shipyard/shipyard/0.61.0/scripts/` has THREE files replaced by unmerged T-39-12 code: `claude-runtime-host.cjs`, `claude-dispatch-adapter.cjs`, `claude-role-host.cjs`, each with a `*.pre-T-39-12.bak` equal to the release (system-state §0).
- Item 0: the only Claude fixture `tests/fixtures/claude-assistant-session.jsonl` is hand-authored (synthetic session ids, no `structured_output`); unit tests stub the CLI with inline `spawn:` closures (`tests/unit/claude-runtime-host.test.cjs:194…422`). T-39-12's new tests keep the pattern (argv-shape assertions, synthetic `{status, summary, output}`), so item 0 is still open after T-39-12. Codex fixtures `tests/fixtures/codex-agent-{child,parent}-0.155.1.jsonl` arrived in #192 (`6995da51`) and carry a CLI version.
- Item 1: `deliver.md:1826` mandates the mark after the verified receipt; `runClaudeDeliveryCli` returns only when the workflow ends (`claude-delivery-host.cjs:903-945`); `stop-gate.cjs:552,582` treats a dispatch as live only via `front.waiting.dispatched`. The sentinel already reserves before launch (`claude-role-host.cjs:748` `reserveRound`, `:959`, `:987`).
- Item 2: no script produces `shipyard.claude-delivery-request.v1` (`grep -rln` → only the host). `deliver.md:1710-1741` makes the model assemble it. The host validates, not derives: `canonicalPlan` requires `realpath(planPath)` under the ticket worktree's graph (`claude-delivery-host.cjs:214-221`); the CLI takes one ticket per process (`:888-900`).
- Item 3: `model-policy-internal.cjs:422-423` accepts `signals.type` only as `facts|alternatives` (ADR-014-INTERFACES.md:12). `deliver.md:1711-1713`, `:2152`, `:2208`, `:2251` tell the orchestrator to pass the ticket `type` inside `signals`. The graph field is the GSD plan kind (`validate-graph.cjs:160` default `implementation`), a different vocabulary with the same name.
- Item 4: `claude-role-host.cjs:168-188` `branchOid` swallows a fetch failure when an expected oid is given (`:176`) and then compares stale refs; `prepareSentinel` (`:485-520`) requires `delivery-state.json` head/base to equal the live PR, which only `state-sync.cjs:587` updates; `deliver.md:2033` requires a clean worktree. The sentinel section (`deliver.md:2020-2060`) names none of this.
- Item 5: `tests/unit/source-contract.test.cjs:1986-1995` pins sha256 of `runtime-adapters.cjs` and `claude-dispatch-adapter.cjs`; no make target, script or doc refreshes it; T-39-12 changed it by hand (`81f3e66c`).
- Item 6: corrected. `validate-graph.cjs:74-86` `slugify(max=40)` yields `…-to-ev`, identical in `tickets.json:5467` and the pushed ref; `git log --all -S'schema-to-eve'` finds nothing. The `…-to-eve` value was model-typed into `gh pr create --head` (transcript [991]). `ticket-worktree.sh` has no slug logic. A second slugify with `max=64` exists for phase dirs (`gsd-sync.cjs:62`). The real gap: nothing derives `--head` from the graph.
- Item 7: `sealPlanningResearch` folds eleven conditions into one message (`claude-delivery-host.cjs:506-516`); `claude-dispatch-adapter.cjs:19` appends a fixed `REPAIR` ("Install an ADR-014-capable Claude host…") to every boundary failure; `investigation-research.mjs:285-290` rethrows any line error, so valid lines lose their seal. The workflow's own validator already has the precise message (`:174`).
- Item 8: `ci-wait.cjs` is the only sanctioned wait (`deliver.md:251`); `run-waker.cjs` wait kinds are `ci, review, quota, lease, host` with no `dispatch` kind; `deliver.md` never mentions harness background launch.
- Item 9: no dogfood mode (`grep -rn -i dogfood` → none); receipts carry no host-source identity (`dispatch-boundary.cjs:1480-1493` records policy_hash, dispatch_id, launch_id only).
- Item 10: `delivery-state.yaml:1` header `# generated by state-sync.cjs …` trips `comment-policy.cjs`; any graph change rewrites ~198 GSD projection files (`SUMMARY|UAT|VERIFICATION`, `shipyard_source_fingerprint`), which made #203's `test-gsd-sync` stale.
- Item 11: `scripts/shipyard-pre-push-gate.sh:13-17` regex-extracts the worktree from command text; replay yields `"/…/inv-003;"` and literal `"$W"`. The hook is installed as a copy (`install-shipyard-claude-hook.sh:236-240`).
- Item 12 (Codex, all confirmed at HEAD): C1 `pipeline-config.cjs:1650-1655` + generic REPAIR `codex-agent.cjs:21,187`, `codex-model-remap.cjs:12`; C2 `gsd-tune.cjs:592` gates only `model_overrides`, `:606-609` `models.*` ungated; C3 research sealer only in `claude-delivery-host.cjs:506,752`, no research branch in `codex-delivery-host.cjs`, research sandbox read-only at `gsd-tune.cjs:117`; C4 `codex-decompose-host.cjs:18`, `codex-dispatch-adapter.cjs:98-99`, pinned by `tests/unit/codex-decompose-host.test.cjs:35,103`; C5 `codex-runtime-host.cjs:638-643` checks no message digest, `:876-880` relays "this exact task"; C6 no `decomposition-result`/`artifact_index` in either decompose host; C7 `codex-decompose-host.cjs:72` state dir undocumented to the agent; C8 `shipyard-doctor.cjs:192-195` reads `<codex home>/shipyard/manifest.json`, which does not exist after install (orchestrator: `ls ~/.codex/shipyard` → `codex-capabilities.json references scripts skills templates workflows`), while the installer writes `~/.codex/agents/.shipyard-manifest.json`.
- Item 13 (PR hygiene, orchestrator): `ticket-pr-match.cjs:5-13,47-56` matches by exact head branch first, then by a `T-NN-NN` title marker or `ticket/<ID>-` branch prefix with title similarity. `role-artifact.cjs:404` requires the executor PR body's first line to be `Ticket: <T>`. `gsd-sync.cjs:870` writes `- Ticket: <T>` into projections. Proving-ground PRs show `T-23-05:` titles, `Ticket:`/`Phase 23 (ADR-024)`/`cascades off ticket/…` body lines, `epic: … integration` titles, and `.planning/phases/…` files in the epic → main diff (#693).

## Constraints

### Technical

- ADR-014 resolver input, grid, receipt shape and fail-closed receipt verification do not change; fixes live in callers, mappings, hosts and messages (`CLAUDE.md`, `ADR-014-INTERFACES.md:6-22`).
- `signals.type` stays `facts | alternatives`; the ticket `type` never flows into it unmapped (`ADR-014-INTERFACES.md:12`, `model-policy-internal.cjs:422`).
- A stop-gate-silencing record needs host-issued launch evidence and must expire (`stop-gate.cjs:167-189`, `dispatch-record.cjs:107-110` TTL 90 min); the mark-after-receipt rule for the durable dispatch record stays.
- Digest pins stay; repair is a deliberate, tested command, never a free-form rewrite (`source-contract.test.cjs:1986-1999`).
- Sentinel identity/base checks stay fail-closed (`claude-role-host.cjs:187,365,513`, ADR-004); the conveyor establishes their preconditions itself.
- The pre-push gate stays fail-closed for real pushes (`shipyard-pre-push-gate.sh:35-37`).
- New code passes the comment policy (`comment-policy.cjs:6-9`); generated files get a sanctioned header or exemption with a test.
- One branch-name producer (`validate-graph.cjs branchFor`); consumers read the graph.
- Codex changes land in canonical sources and are regenerated; parity is proven by `make test-codex-shipyard` (network).
- Scripts run on macOS bash 3.2 + Node 24 with no npm deps and no `timeout`; waits live in Node or background processes, not foreground sleeps.
- CI is offline (`.github/workflows/test.yml:48-55`); captured fixtures are committed and replayed; no CI step calls `claude`/`codex`.
- Unit fixtures that create git repos must be hermetic against global `commit.gpgsign` and fixed `/tmp` paths (constraints C-T15: sandboxed `make test-fast` failed on gpg and `mkdtemp /tmp/scds-XXXXXX`).

### Product

- Claude plugin is canonical; Codex regenerated after every shared change.
- Refusals keep codes, exit status and fail-closed behaviour; hints come from the one T-39-01 code→hint map.
- The stop gate enforces only in a deliver-armed session (T-39-03); item 1 builds on that marker.
- Research handback bound is 500 characters (T-39-08); per-line handling reuses it.
- Installers write only to the selected runtime homes; `make doctor` stays read-only.
- README, `plugin.json` and `tests/smoke/docs-smoke.sh` change together when the command surface changes.
- PR hygiene applies to target projects; the Shipyard repository's own PRs describe the conveyor and are exempt.

### Delivery

- CI runs the publish gate and `make test-fast`; `make test` adds network-bound Codex and release checks.
- Every planning change commits refreshed GSD projections in the same PR (`Makefile:65-66`).
- Tickets overlapping phase-39 files take a cross-phase dependency and wait for the phase 39 epic on `main` (`state-sync.cjs:811`); the rest start from `main`. Most items overlap (risks F3 / constraints C-D5 overlap map).
- Installer/hook changes reach users only after reinstall; acceptance runs start with `make install-shipyard-claude-hook`, `make install-shipyard-codex`, `make doctor`.
- The worktree HEAD must not move while a research fan-out runs: the sealer compares `HEAD` with `sourceRevision` (`claude-delivery-host.cjs:521`); found by the orchestrator in this investigation.

## Unknowns

- Whether the Claude workflow runtime exposes a launch id before completion (item 1) → resolved by design: the host itself writes the in-flight record at launch (DECISIONS).
- Whether Codex exposes the actual `spawn_agent` message to the host (C5) → carried as RISKS "C5 relay verification".
- Exact path-scoped write support in the Codex sandbox (C4) → carried as RISKS "C4 researcher write scope".
